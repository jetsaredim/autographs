---
phase: 03-public-gallery-mvp
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/app/layout.tsx
  - app/app/page.tsx
  - app/app/globals.css
  - app/app/components/PublicHeader.tsx
  - app/app/components/PublicFooter.tsx
  - app/app/components/AdminUnlock.tsx
  - app/app/admin/page.tsx
requirements:
  - GALL-01
autonomous: true
must_haves:
  truths:
    - "Anonymous visitors land on Jared Greenwald's Autograph Gallery rather than the proof-of-life page."
    - "Landing page offers only View Collection and Surprise Me as primary actions."
    - "Surprise Me appears only on `/` and chooses from the full published collection."
    - "Public pages expose a tiny About footer link to `/architecture` and no visible Admin navigation."
    - "A non-obvious operator affordance reaches an admin coming-soon page with no auth or mutation workflow."
  artifacts:
    - path: "app/app/page.tsx"
      provides: "Branded public landing page"
    - path: "app/app/layout.tsx"
      provides: "Public metadata title/description"
    - path: "app/app/components/PublicHeader.tsx"
      provides: "Shared restrained public brand/navigation shell"
    - path: "app/app/components/PublicFooter.tsx"
      provides: "Tiny About footer link"
    - path: "app/app/components/AdminUnlock.tsx"
      provides: "Hidden/non-obvious admin unlock affordance"
    - path: "app/app/admin/page.tsx"
      provides: "Admin coming-soon page only"
    - path: "app/app/globals.css"
      provides: "Native CSS tokens and landing/admin/chrome styles"
  key_links:
    - from: "app/app/page.tsx"
      to: "app/src/catalog/public-view-models.ts"
      via: "public image path/view model helpers for landing visual and random item IDs"
      pattern: "publicImagePath|toPublicGalleryItem"
    - from: "app/app/components/PublicFooter.tsx"
      to: "/architecture"
      via: "About link"
      pattern: "href=\"/architecture\""
---

<objective>
Replace the proof-of-life public shell with the branded landing page, footer, and hidden admin access affordance.

Purpose: Establish the public first impression and navigation boundaries required before collection/detail pages are added.
Output: Updated root metadata, landing page, shared public chrome, hidden admin unlock, admin coming-soon page, and native CSS tokens.
</objective>

<execution_context>
@/home/jgreenwa/.codex/get-shit-done/workflows/execute-plan.md
@/home/jgreenwa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-public-gallery-mvp/03-CONTEXT.md
@.planning/phases/03-public-gallery-mvp/03-RESEARCH.md
@.planning/phases/03-public-gallery-mvp/03-PATTERNS.md
@.planning/phases/03-public-gallery-mvp/03-UI-SPEC.md
@.planning/phases/03-public-gallery-mvp/03-01-SUMMARY.md
@app/app/page.tsx
@app/app/layout.tsx
@app/app/globals.css
@app/app/architecture/page.tsx
@app/src/catalog/index.ts
@app/src/catalog/public-view-models.ts

<interfaces>
Use `createCatalogService().list()` server-side for full published collection reads. Use public view-model helpers from Plan 03-01 before data crosses into Client Components. All images rendered by this plan must use `/api/catalog/{itemId}/images/{imageId}`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update public metadata, chrome, footer, and native CSS tokens</name>
  <read_first>
    - app/app/layout.tsx
    - app/app/globals.css
    - app/app/architecture/page.tsx
    - .planning/phases/03-public-gallery-mvp/03-UI-SPEC.md
  </read_first>
  <files>
    app/app/layout.tsx
    app/app/globals.css
    app/app/components/PublicHeader.tsx
    app/app/components/PublicFooter.tsx
  </files>
  <acceptance_criteria>
    - Metadata title and description use `Jared Greenwald's Autograph Gallery`.
    - Global CSS uses UI-SPEC colors `#f8f7f4`, `#ffffff`, `#1f6f68`, `#1f2937`, `#4b5563`, `#d7d2c8`, and `#f1efea`.
    - No decorative gradients, gradient orbs, bokeh blobs, negative letter spacing, viewport-scaled font sizes, or card radii above 8px are introduced for Phase 3 public surfaces.
    - Footer includes only a small About link to `/architecture` and does not include an Admin link.
  </acceptance_criteria>
  <action>Replace proof-of-life styling with the Phase 3 native CSS token system from the UI-SPEC. Create `PublicHeader` and `PublicFooter` components with restrained public branding, a collection link where appropriate, and the tiny About footer link to `/architecture` per D-03-47 and D-03-48. Keep public navigation sparse and do not expose an Admin nav target.</action>
  <verify>
    <automated>corepack pnpm --filter app typecheck</automated>
    <automated>rg -n "Jared Greenwald's Autograph Gallery|#1f6f68|/architecture" app/app/layout.tsx app/app/globals.css app/app/components/PublicFooter.tsx</automated>
    <automated>! rg -n "gradient|letter-spacing:\\s*-" app/app/globals.css</automated>
  </verify>
  <done>Shared public chrome and design tokens match the approved Phase 3 public-gallery contract.</done>
</task>

<task type="auto">
  <name>Task 2: Build branded landing page with View Collection and Surprise Me</name>
  <read_first>
    - app/app/page.tsx
    - app/src/catalog/index.ts
    - app/src/catalog/public-view-models.ts
    - .planning/phases/03-public-gallery-mvp/03-CONTEXT.md
    - .planning/phases/03-public-gallery-mvp/03-UI-SPEC.md
  </read_first>
  <files>
    app/app/page.tsx
    app/app/globals.css
  </files>
  <acceptance_criteria>
    - `/` is a basic overview, not the full collection grid.
    - First viewport shows `Jared Greenwald's Autograph Gallery`, one real published-item image-backed visual signal when data exists, `View Collection`, and `Surprise Me`.
    - `View Collection` links to `/collection`.
    - `Surprise Me` exists only in `app/app/page.tsx`, selects from the full published collection, and navigates to `/collection/{itemId}` without honoring collection filters.
    - Landing image display prevents the default context menu and uses only app-mediated image route paths.
  </acceptance_criteria>
  <action>Replace the proof-of-life root page per D-03-01 through D-03-04. Fetch published items server-side with `createCatalogService().list()` and shape them through public view-model helpers. Render the brand, concise overview copy, a non-grid visual feature using a published primary image when available, and exactly the two primary actions `View Collection` and `Surprise Me`. Implement Surprise Me with a small Client Component or server-rendered safe data payload containing only published item IDs and public image references; do not add Surprise Me to collection routes.</action>
  <verify>
    <automated>corepack pnpm --filter app typecheck</automated>
    <automated>corepack pnpm --filter app lint</automated>
    <automated>rg -n "View Collection|Surprise Me|/collection|onContextMenu" app/app/page.tsx app/app/globals.css</automated>
    <automated>! rg -n "objectKey|bucketName|storageNamespace|Object Storage|signed URL|Admin" app/app/page.tsx</automated>
  </verify>
  <done>The root route is the branded landing experience with only the locked public actions and no storage leakage.</done>
</task>

<task type="auto">
  <name>Task 3: Add hidden admin unlock and admin coming-soon page</name>
  <read_first>
    - .planning/phases/03-public-gallery-mvp/03-CONTEXT.md
    - .planning/phases/03-public-gallery-mvp/03-UI-SPEC.md
    - app/app/architecture/page.tsx
  </read_first>
  <files>
    app/app/components/AdminUnlock.tsx
    app/app/admin/page.tsx
    app/app/page.tsx
    app/app/globals.css
  </files>
  <acceptance_criteria>
    - Admin affordance is non-obvious and operator-discoverable, such as a keyboard combination or nearly hidden brand/footer affordance.
    - No visible public navigation label says `Admin`.
    - `/admin` communicates collection management is coming later.
    - `/admin` includes no login form, auth handling, mutation controls, upload controls, edit controls, publish controls, operator endpoint forms, or token entry.
  </acceptance_criteria>
  <action>Implement D-03-49 through D-03-52. Add a tiny `AdminUnlock` Client Component that navigates to `/admin` only through the chosen hidden affordance. Add `app/app/admin/page.tsx` as static informational content only. Keep it separate from `/api/operator/...` and do not create login, mutation, upload, edit, publish, or operator workflow behavior.</action>
  <verify>
    <automated>corepack pnpm --filter app typecheck</automated>
    <automated>corepack pnpm --filter app lint</automated>
    <automated>rg -n "/admin|coming later|management" app/app/components/AdminUnlock.tsx app/app/admin/page.tsx</automated>
    <automated>! rg -n "form|password|login|upload|edit|publish|operator|AUTOGRAPHS_OPERATOR" app/app/admin/page.tsx app/app/components/AdminUnlock.tsx</automated>
  </verify>
  <done>The future admin access pattern exists without exposing a visible public Admin nav or any Phase 4 behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous browser to landing page | Public visitors receive only published item IDs and app-mediated image paths. |
| Hidden unlock to `/admin` page | Obscurity affordance must not be treated as authentication or expose mutation behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | `app/app/page.tsx` | mitigate | Use public view-model helpers and grep for storage metadata terms. |
| T-03-02-02 | Spoofing | `AdminUnlock.tsx` and `/admin` | accept | Unlock is not authentication; `/admin` is informational only with no privileged action. |
| T-03-02-03 | Elevation of Privilege | `app/app/admin/page.tsx` | mitigate | Do not include login, mutation, upload, edit, publish, operator token, or API forms. |
| T-03-02-04 | Repudiation | image friction copy | mitigate | Do not claim DRM; implement context-menu prevention only as anti-casual friction. |
</threat_model>

<verification>
Run `corepack pnpm --filter app typecheck`, `corepack pnpm --filter app lint`, and source assertions for public brand, CTA copy, absence of visible Admin nav, and absence of storage metadata in landing/admin files.
</verification>

<success_criteria>
Anonymous visitors see the branded landing page with the locked primary actions, footer, and non-obvious admin affordance while privileged workflows remain absent.
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-gallery-mvp/03-02-SUMMARY.md`.
</output>
