---
phase: 03-public-gallery-mvp
plan: 05
type: execute
wave: 5
depends_on:
  - 03-02
  - 03-03
  - 03-04
files_modified:
  - docs/temporary-production-data-entry.md
  - docs/deployment-runbook.md
requirements:
  - GALL-01
  - GALL-02
  - GALL-03
  - GALL-04
autonomous: true
must_haves:
  truths:
    - "Operator has a documented temporary production data-entry procedure through an SSH tunnel to local runtime endpoints."
    - "Operator docs explicitly avoid public Caddy exposure for `/api/operator/...` routes."
    - "Final Phase 3 verification checks public browse, filter, detail, multi-image, quote, footer/admin, and media privacy invariants."
  artifacts:
    - path: "docs/temporary-production-data-entry.md"
      provides: "Temporary operator data-entry runbook through SSH local port forwarding"
    - path: "docs/deployment-runbook.md"
      provides: "Link from deployment docs to the temporary data-entry procedure"
  key_links:
    - from: "docs/temporary-production-data-entry.md"
      to: "/api/operator/catalog"
      via: "local SSH tunnel only"
      pattern: "127.0.0.1"
    - from: "docs/temporary-production-data-entry.md"
      to: "AUTOGRAPHS_OPERATOR_API_TOKEN"
      via: "Authorization bearer token requirement"
      pattern: "Authorization: Bearer"
---

<objective>
Document the transitional production data-entry path and run final Phase 3 privacy/behavior gates.

Purpose: Preserve the operator bridge until the later admin workflow exists without exposing operator endpoints publicly, and close the phase with explicit public gallery verification.
Output: Temporary data-entry runbook, deployment-runbook pointer, and final verification evidence in the plan summary.
</objective>

<execution_context>
@/home/jgreenwa/.codex/get-shit-done/workflows/execute-plan.md
@/home/jgreenwa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-public-gallery-mvp/03-CONTEXT.md
@.planning/phases/03-public-gallery-mvp/03-RESEARCH.md
@.planning/phases/03-public-gallery-mvp/03-UI-SPEC.md
@.planning/phases/03-public-gallery-mvp/03-01-SUMMARY.md
@.planning/phases/03-public-gallery-mvp/03-02-SUMMARY.md
@.planning/phases/03-public-gallery-mvp/03-03-SUMMARY.md
@.planning/phases/03-public-gallery-mvp/03-04-SUMMARY.md
@docs/deployment-runbook.md
@docs/configuration-contract.md
@app/app/api/operator/catalog/route.ts
@app/app/api/operator/catalog/[id]/route.ts

<interfaces>
Existing operator routes are token-guarded and transitional. Documentation may mention `/api/operator/...` only through `http://127.0.0.1:<forwarded-port>` after SSH local port forwarding to `127.0.0.1:3000` on the runtime VM. Do not document public Caddy exposure for operator routes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add temporary production data-entry runbook</name>
  <read_first>
    - docs/deployment-runbook.md
    - docs/configuration-contract.md
    - app/app/api/operator/catalog/route.ts
    - app/app/api/operator/catalog/[id]/route.ts
    - .planning/phases/03-public-gallery-mvp/03-CONTEXT.md
  </read_first>
  <files>
    docs/temporary-production-data-entry.md
  </files>
  <acceptance_criteria>
    - Runbook documents `ssh -L <local-port>:127.0.0.1:3000 opc@<runtime-public-ip>`.
    - Runbook shows curl/script calls to `http://127.0.0.1:<forwarded-port>/api/operator/...`.
    - Runbook requires `Authorization: Bearer <AUTOGRAPHS_OPERATOR_API_TOKEN>`.
    - Runbook says to write metadata and media through existing catalog service/operator API path, not manual database edits or untracked object uploads.
    - Runbook explicitly says not to expose operator endpoints through public Caddy routes.
  </acceptance_criteria>
  <action>Create `docs/temporary-production-data-entry.md` for D-03-32 through D-03-37. Frame the procedure as a transitional operator path until the later collection-management workflow exists. Include preconditions, SSH tunnel command shape, bearer-token curl shape, expected use of existing operator endpoints, and warnings against manual database/object edits or public Caddy exposure.</action>
  <verify>
    <automated>rg -n "ssh -L|127\\.0\\.0\\.1|/api/operator|Authorization: Bearer|AUTOGRAPHS_OPERATOR_API_TOKEN|Caddy|manual" docs/temporary-production-data-entry.md</automated>
  </verify>
  <done>Operator has a clear temporary production data-entry procedure that preserves private service boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Link the temporary runbook from deployment docs</name>
  <read_first>
    - docs/deployment-runbook.md
    - docs/temporary-production-data-entry.md
  </read_first>
  <files>
    docs/deployment-runbook.md
  </files>
  <acceptance_criteria>
    - Deployment runbook references `docs/temporary-production-data-entry.md` from the data/media or operations section.
    - Link text makes clear the procedure is tunnel-only and transitional.
    - Deployment runbook does not instruct public exposure of `/api/operator/...`.
  </acceptance_criteria>
  <action>Add a concise pointer from `docs/deployment-runbook.md` to `docs/temporary-production-data-entry.md`, near the existing data/media smoke or operational notes. Keep the wording aligned with D-03-36: operator endpoints remain off public Caddy routes.</action>
  <verify>
    <automated>rg -n "temporary-production-data-entry|tunnel|/api/operator|Caddy" docs/deployment-runbook.md</automated>
    <automated>! rg -n "Caddy.*operator|operator.*public|public.*operator" docs/deployment-runbook.md docs/temporary-production-data-entry.md</automated>
  </verify>
  <done>Deployment docs point operators to the temporary data-entry procedure without widening public route exposure.</done>
</task>

<task type="auto">
  <name>Task 3: Run final public gallery gates and record evidence</name>
  <read_first>
    - app/app/page.tsx
    - app/app/collection/page.tsx
    - app/app/collection/[id]/page.tsx
    - app/app/components/GalleryGrid.tsx
    - app/app/components/GalleryFilters.tsx
    - app/app/components/ImageViewer.tsx
    - app/src/catalog/public-view-models.ts
    - app/src/gallery/approved-quotes.ts
    - docs/temporary-production-data-entry.md
  </read_first>
  <files>
    docs/temporary-production-data-entry.md
    docs/deployment-runbook.md
  </files>
  <acceptance_criteria>
    - Typecheck, lint, build, and seed dry-run pass.
    - Source assertions find public brand, View Collection, Surprise Me only on landing, `/collection`, detail route, About link, hidden admin unlock, approved quotes, and app-mediated image paths.
    - Source assertions find no direct Object Storage URLs, object keys, storage identifiers, bucket credentials, or operator token usage in public UI files.
    - Summary records any live cloud verification not run locally as an operator gate rather than pretending it passed.
  </acceptance_criteria>
  <action>Run final validation for GALL-01 through GALL-04 and the Phase 3 privacy constraints. Include evidence in `.planning/phases/03-public-gallery-mvp/03-05-SUMMARY.md`: commands run, pass/fail results, any manual browser smoke still recommended, and any operator-only live checks not run due missing credentials.</action>
  <verify>
    <automated>corepack pnpm --filter app typecheck</automated>
    <automated>corepack pnpm --filter app lint</automated>
    <automated>corepack pnpm --filter app build</automated>
    <automated>corepack pnpm --filter app db:seed:dry-run</automated>
    <automated>rg -n "Jared Greenwald's Autograph Gallery|View Collection|Surprise Me|/collection|/architecture|approved|/api/catalog/.*/images" app/app app/src/catalog/public-view-models.ts app/src/gallery/approved-quotes.ts</automated>
    <automated>! rg -n "objectKey|bucketName|storageNamespace|OCI|Object Storage|AUTOGRAPHS_OPERATOR|signed URL|includeUnpublished" app/app/page.tsx app/app/collection app/app/components app/src/catalog/public-view-models.ts app/src/gallery/approved-quotes.ts</automated>
  </verify>
  <done>Phase 3 has a documented operator bridge and final verification evidence for public gallery behavior and privacy invariants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator workstation to runtime VM | Temporary production data entry crosses from local machine through SSH tunnel to app-local endpoints. |
| Public internet to Caddy | Operator mutation routes must not be documented or configured as public Caddy exposure. |
| Final source verification to public UI | Source assertions guard against accidental public leakage of storage or operator identifiers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Elevation of Privilege | operator docs | mitigate | Require SSH local port forwarding and bearer token; forbid public Caddy exposure. |
| T-03-05-02 | Tampering | production data entry | mitigate | Use existing operator catalog service route so metadata and media remain linked; forbid manual database rows or untracked object uploads. |
| T-03-05-03 | Information Disclosure | public UI source | mitigate | Final grep gate for storage metadata, direct Object Storage strings, operator token references, and unpublished reads. |
| T-03-05-04 | Repudiation | verification summary | mitigate | Record exact commands and distinguish local pass from operator-only live checks. |
</threat_model>

<verification>
Run the automated commands listed in Task 3. The executor should additionally manual-smoke `/`, `/collection`, filtered `/collection`, one published detail page, thumbnail swapping, missing detail, footer About link, and hidden admin unlock if a local dev server is available.
</verification>

<success_criteria>
Phase 3 closes with anonymous public browse/filter/detail/image behavior implemented, operator data entry kept tunnel-only, and image/privacy invariants checked against source and build output.
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-gallery-mvp/03-05-SUMMARY.md`.
</output>
