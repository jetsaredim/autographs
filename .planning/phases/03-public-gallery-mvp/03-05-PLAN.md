---
phase: 03-public-gallery-mvp
plan: 05
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
files_modified:
  - app/src/gallery/public-surface.test.ts
  - docs/temporary-production-data-entry.md
  - docs/deployment-runbook.md
autonomous: true
requirements:
  - GALL-01
  - GALL-02
  - GALL-03
  - GALL-04
must_haves:
  truths:
    - "Automated checks verify public Phase 3 source does not expose private Object Storage fields or direct storage URL patterns."
    - "The operator has committed documentation for temporary production data entry through an SSH tunnel and token-guarded operator API calls."
    - "The public Caddy path does not intentionally expose operator endpoints in Phase 3 documentation."
    - "Final validation runs lint, typecheck, unit tests, and build."
  artifacts:
    - path: "app/src/gallery/public-surface.test.ts"
      provides: "Source-level privacy and route-scope regression tests"
    - path: "docs/temporary-production-data-entry.md"
      provides: "Operator-only temporary production data entry procedure"
    - path: "docs/deployment-runbook.md"
      provides: "Link to temporary data-entry procedure and public media invariant"
  key_links:
    - from: "docs/deployment-runbook.md"
      to: "docs/temporary-production-data-entry.md"
      via: "Markdown link"
      pattern: "temporary-production-data-entry"
    - from: "app/src/gallery/public-surface.test.ts"
      to: "app/app"
      via: "source scan"
      pattern: "storageNamespace|bucketName|objectKey"
---

<objective>
Close Phase 3 with privacy regression tests, operator-only temporary data-entry documentation, and full validation.

Purpose: Public browsing must not leak private media implementation details, and the production data-entry bridge must be documented without becoming a public workflow.
Output: Source privacy tests, temporary data-entry runbook, deployment-runbook link, and final validation summary.
</objective>

<execution_context>
@/home/jgreenwa/.codex/get-shit-done/workflows/execute-plan.md
@/home/jgreenwa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-public-gallery-mvp/03-CONTEXT.md
@.planning/phases/03-public-gallery-mvp/03-RESEARCH.md
@.planning/phases/03-public-gallery-mvp/03-UI-SPEC.md
@app/app/api/catalog/[id]/images/[imageId]/route.ts
@app/app/api/operator/catalog/route.ts
@docs/deployment-runbook.md
@app/package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add public surface privacy regression tests</name>
  <files>app/src/gallery/public-surface.test.ts</files>
  <read_first>
    - app/package.json
    - app/src/catalog/public-view-models.ts
    - app/app/api/catalog/[id]/images/[imageId]/route.ts
    - app/app/api/operator/catalog/route.ts
    - app/src/gallery/public-surface.test.ts
    - .planning/phases/03-public-gallery-mvp/03-CONTEXT.md
  </read_first>
  <behavior>
    - Test: files under `app/app` excluding `app/app/api/operator/**` do not contain `storageNamespace`, `bucketName`, `objectKey`, `OCI_`, `MEDIA_BUCKET`, or `https://objectstorage`.
    - Test: public UI files do not contain visible `href` links to `/api/catalog/{itemId}/images/{imageId}` as standalone anchors.
    - Test: collection page source does not contain `Surprise Me`.
    - Test: admin placeholder files do not contain mutation/auth workflow strings `POST`, `PUT`, `PATCH`, `DELETE`, `Authorization`, `Bearer`, `upload`, `publish`, `password`, or `login`.
  </behavior>
  <action>Create `app/src/gallery/public-surface.test.ts` using Node's built-in `node:test`, `node:assert/strict`, and `node:fs/promises`. Scan Phase 3 public UI files and enforce the privacy/source gates listed in the behavior block. Exclude existing server-only catalog, media, db, and operator route implementations from storage-field scans where those fields are legitimate. Keep the tests deterministic and runnable through `corepack pnpm --filter app test`.</action>
  <verify>
    <automated>corepack pnpm --filter app test -- --test-name-pattern "public surface"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `app/src/gallery/public-surface.test.ts` contains `node:test`.
    - Behavior assertion: `corepack pnpm --filter app test -- --test-name-pattern "public surface"` passes.
    - Source assertion: the test file includes the exact deny-list strings `storageNamespace`, `bucketName`, `objectKey`, `https://objectstorage`, `Authorization`, and `Bearer`.
  </acceptance_criteria>
  <done>Automated source checks guard against storage leakage, visible admin nav/workflows, and collection-page Surprise Me regression.</done>
</task>

<task type="auto">
  <name>Task 2: Document temporary production data entry through SSH tunnel</name>
  <files>docs/temporary-production-data-entry.md, docs/deployment-runbook.md</files>
  <read_first>
    - docs/deployment-runbook.md
    - app/app/api/operator/catalog/route.ts
    - app/app/api/operator/catalog/[id]/route.ts
    - .planning/phases/03-public-gallery-mvp/03-CONTEXT.md
    - .planning/phases/02-oracle-and-private-media-core/02-04-SUMMARY.md
  </read_first>
  <action>Create `docs/temporary-production-data-entry.md` with sections `Purpose`, `Security Boundary`, `SSH Tunnel`, `Token-Guarded Calls`, `Media and Metadata Path`, `What Not To Do`, and `Retirement Path`. Include the exact tunnel command shape `ssh -L <local-port>:127.0.0.1:3000 opc@<runtime-public-ip>`. Include the exact curl header shape `Authorization: Bearer <AUTOGRAPHS_OPERATOR_API_TOKEN>` and URL shape `http://127.0.0.1:<forwarded-port>/api/operator/...`. State explicitly that operator endpoints must not be exposed through public Caddy routes in Phase 3, and that operators must not hand-edit Oracle rows or upload untracked Object Storage objects. Add a short link from `docs/deployment-runbook.md` to the new document near `Data and Media Smoke` or an operator section.</action>
  <verify>
    <automated>corepack pnpm --filter app lint</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `docs/temporary-production-data-entry.md` contains `ssh -L <local-port>:127.0.0.1:3000 opc@<runtime-public-ip>`.
    - Source assertion: `docs/temporary-production-data-entry.md` contains `Authorization: Bearer <AUTOGRAPHS_OPERATOR_API_TOKEN>`.
    - Source assertion: `docs/temporary-production-data-entry.md` contains `Operator endpoints must not be exposed through public Caddy routes`.
    - Source assertion: `docs/temporary-production-data-entry.md` contains `Do not hand-edit Oracle rows` and `Do not upload untracked Object Storage objects`.
    - Source assertion: `docs/deployment-runbook.md` links to `temporary-production-data-entry.md`.
    - Schema assertion: no files under `app/db/migrations/` are modified; this plan intentionally avoids schema changes and therefore does not run `corepack pnpm --filter app db:migrate`.
  </acceptance_criteria>
  <done>Temporary production data entry is documented as an operator-only, tunneled, token-guarded bridge until Phase 4.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 3 validation</name>
  <files>app/src/gallery/public-surface.test.ts, docs/temporary-production-data-entry.md, docs/deployment-runbook.md</files>
  <read_first>
    - app/package.json
    - app/src/gallery/public-surface.test.ts
    - docs/temporary-production-data-entry.md
    - docs/deployment-runbook.md
    - .planning/phases/03-public-gallery-mvp/03-CONTEXT.md
    - .planning/phases/03-public-gallery-mvp/03-UI-SPEC.md
  </read_first>
  <action>Run the complete validation set after Plans 01 through 04 are complete: `corepack pnpm --filter app lint`, `corepack pnpm --filter app typecheck`, `corepack pnpm --filter app test`, and `corepack pnpm --filter app build`. If any command fails, fix only Phase 3-related failures and rerun the failing command plus any downstream command. Record final command outcomes in the summary. Do not run database migrations because this phase stores approved quotes in a durable TypeScript source and makes no schema changes.</action>
  <verify>
    <automated>corepack pnpm --filter app lint</automated>
    <automated>corepack pnpm --filter app typecheck</automated>
    <automated>corepack pnpm --filter app test</automated>
    <automated>corepack pnpm --filter app build</automated>
  </verify>
  <acceptance_criteria>
    - CLI assertion: `corepack pnpm --filter app lint` exits 0.
    - CLI assertion: `corepack pnpm --filter app typecheck` exits 0.
    - CLI assertion: `corepack pnpm --filter app test` exits 0.
    - CLI assertion: `corepack pnpm --filter app build` exits 0.
    - Schema assertion: `git diff --name-only -- app/db/migrations` returns no files for Phase 3; therefore `corepack pnpm --filter app db:migrate` is not required.
  </acceptance_criteria>
  <done>Phase 3 passes static, type, unit, privacy, and production build validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public source bundle -> anonymous browser | Rendered/public code must not contain storage identifiers or operator workflows. |
| Operator workstation -> runtime VM app | Temporary production data entry crosses through SSH tunnel and bearer-token-guarded operator endpoints. |
| Public Caddy -> app container | Public reverse proxy must not be treated as an operator API exposure path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | I | Public source files | mitigate | Source regression test scans public UI for storage fields, Object Storage URLs, and OCI/media secret names. |
| T-03-05-02 | I | Direct Object Storage URL/key leakage | mitigate | Regression tests enforce app-mediated image route invariant and deny direct storage patterns. |
| T-03-05-03 | I | Anti-casual image extraction limits | mitigate | Validation checks public UI has no standalone direct image links and prior plans add context-menu friction. |
| T-03-05-04 | E | Operator endpoints | mitigate | Docs require SSH tunnel to `127.0.0.1:3000`, bearer token, and no public Caddy exposure. |
| T-03-05-05 | T | Production data entry | mitigate | Docs require writing through existing operator API/catalog service, not manual Oracle row edits or untracked object uploads. |
| T-03-05-06 | I | Hidden admin placeholder exposure | mitigate | Regression tests deny login/mutation/upload/publish strings in placeholder files. |
| T-03-05-07 | D | Final validation | mitigate | Full lint/typecheck/test/build gate blocks shipping broken public routes. |
</threat_model>

<verification>
Run `corepack pnpm --filter app lint`, `corepack pnpm --filter app typecheck`, `corepack pnpm --filter app test`, and `corepack pnpm --filter app build`.
</verification>

<success_criteria>
Phase 3 has automated privacy gates, documented operator-only production data entry, and green final validation without schema changes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-gallery-mvp/03-05-SUMMARY.md`.
</output>
