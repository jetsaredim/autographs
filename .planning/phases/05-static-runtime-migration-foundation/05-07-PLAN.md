---
phase: 05-static-runtime-migration-foundation
plan: 07
type: execute
wave: 7
depends_on:
  - 05-06
files_modified:
  - controller/tests/live_static_publish_smoke.rs
  - docs/deployment-runbook.md
  - docs/static-runtime-runbook.md
  - docs/temporary-production-data-entry.md
  - docs/configuration-contract.md
autonomous: false
requirements:
  - STATIC-02
  - STATIC-04
  - STATIC-05
  - STATIC-06
  - STATIC-07
must_haves:
  truths:
    - "A credential-gated live smoke proves the seed-to-published-static path through Oracle, Object Storage, the controller, generated derivatives, and local/private Caddy validation."
    - "Cutover and retirement criteria are documented for Next.js public runtime, public catalog APIs, app-mediated streaming, data smoke, and the Node operator bridge."
  artifacts:
    - path: "controller/tests/live_static_publish_smoke.rs"
      provides: "Credential-gated live seed-to-published-static proof"
    - path: "docs/static-runtime-runbook.md"
      provides: "Publish, validation, cutover, full rebuild, and retirement guidance"
  key_links:
    - from: "controller/tests/live_static_publish_smoke.rs"
      to: "deploy/ansible/roles/autographs_deploy/files/Caddyfile"
      via: "local/private candidate validation requests"
    - from: "docs/static-runtime-runbook.md"
      to: "controller/tests/live_static_publish_smoke.rs"
      via: "mandatory operator-run live verification command"
---

<objective>
Prove the live seed-to-published-static runtime and document cutover.

Purpose: Close Phase 5 with one operator-run end-to-end proof and explicit retirement guidance after deploy wiring exists.
Output: Credential-gated live static publish smoke and cutover/retirement runbooks.
</objective>

<execution_context>
@/home/jgreenwa/.codex/get-shit-done/workflows/execute-plan.md
@/home/jgreenwa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/05-static-runtime-migration-foundation/05-CONTEXT.md
@controller/tests/live_persistence_smoke.rs
@controller/src/routes.rs
@controller/src/publisher.rs
@deploy/ansible/roles/autographs_deploy/files/Caddyfile
@docs/deployment-runbook.md
@docs/temporary-production-data-entry.md
@docs/configuration-contract.md
</context>

<tasks>

<task type="manual">
  <name>Task 1: Add live seed-to-published-static validation smoke</name>
  <read_first>
    - controller/tests/live_persistence_smoke.rs
    - controller/src/routes.rs
    - controller/src/publisher.rs
    - deploy/ansible/roles/autographs_deploy/files/Caddyfile
  </read_first>
  <files>controller/tests/live_static_publish_smoke.rs, docs/static-runtime-runbook.md</files>
  <action>Add a credential-gated live smoke that runs only when `AUTOGRAPHS_LIVE_STATIC_PUBLISH_SMOKE=true` and required controller/admin, Oracle, OCI Object Storage, static release root, and local/private Caddy validation configuration are present. Seed one uniquely named item and image through `/admin/api/*`, verify Oracle/Object Storage persistence, trigger publish, validate the candidate release, and fetch the generated item page, JSON, and thumbnail/detail derivatives through local/private Caddy without private identifier leakage. Skip explicitly when credentials are absent and require the operator-run smoke before Phase 5 verification passes.</action>
  <verify><manual>AUTOGRAPHS_LIVE_STATIC_PUBLISH_SMOKE=true cargo test --manifest-path controller/Cargo.toml live_static_publish_smoke -- --ignored --nocapture</manual></verify>
  <acceptance_criteria>
    - Smoke skips with an explicit message unless `AUTOGRAPHS_LIVE_STATIC_PUBLISH_SMOKE=true`.
    - Smoke seeds one item and image through `/admin/api/*`, triggers publish, and validates the candidate or current release.
    - Smoke verifies a generated public item page, JSON data, and thumbnail/detail derivative URLs through local/private Caddy.
    - Fetched content is scanned for `storageNamespace`, `bucketName`, `objectKey`, `https://objectstorage`, and private image UUID leakage.
  </acceptance_criteria>
  <done>Live smoke proves one private seed/upload becomes validated public static HTML, JSON, and derivative media through the local/private Caddy path.</done>
</task>

<task type="manual">
  <name>Task 2: Document cutover, validation, and old path retirement</name>
  <read_first>
    - docs/deployment-runbook.md
    - docs/temporary-production-data-entry.md
    - docs/configuration-contract.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>docs/static-runtime-runbook.md, docs/deployment-runbook.md, docs/temporary-production-data-entry.md, docs/configuration-contract.md</files>
  <action>Document deploy preflight, seed/upload, incremental publish, full rebuild, candidate validation, mandatory live smoke, planned downtime cutover, failed-candidate inspection, and route retirement checks. Mark the public Next.js runtime, public catalog APIs, app-mediated image streaming, old data smoke path, and temporary Node operator bridge with explicit retirement criteria or current-until-cutover labels.</action>
  <verify><automated>rg -n "cutover|retire|/api/catalog|/api/operator|data smoke|full rebuild|candidate validation|planned downtime|live static publish smoke" docs/static-runtime-runbook.md docs/deployment-runbook.md docs/temporary-production-data-entry.md docs/configuration-contract.md</automated></verify>
  <acceptance_criteria>
    - Runbook contains `Candidate validation`, `Cutover`, `Full rebuild`, `Live static publish smoke`, and `Retirement checks` sections.
    - Docs mention retirement criteria for `/api/catalog/*`, `/api/operator/*`, app-mediated image streaming, and data smoke workflow.
    - Docs state planned downtime is acceptable and rollback is roll-forward/full-rebuild oriented.
  </acceptance_criteria>
  <done>Operator docs define verification, planned cutover, roll-forward recovery, and retirement criteria for every superseded runtime path.</done>
</task>

</tasks>

<verification>
Run the credential-gated live static publish smoke against the deployed runtime and review the cutover checklist before retiring old paths.
</verification>

<success_criteria>
STATIC-02, STATIC-04, STATIC-05, STATIC-06, and STATIC-07 have a mandatory live end-to-end proof plus explicit operator cutover and retirement guidance.
</success_criteria>

<output>
Create `.planning/phases/05-static-runtime-migration-foundation/05-07-SUMMARY.md` when done.
</output>
