---
phase: 08-admin-media-review-and-operational-posture
plan: "07"
type: execute
wave: 7
depends_on:
  - "08-06"
files_modified:
  - controller/src/publisher.rs
  - controller/tests/publisher.rs
  - controller/tests/static_contract.rs
  - controller/tests/live_static_publish_smoke.rs
  - docs/static-artifact-contract.md
  - docs/static-runtime-runbook.md
autonomous: true
requirements:
  - MEDIA-05
  - MEDIA-06
must_haves:
  truths:
    - "Static publishing applies saved image adjustments before resizing and encoding public derivatives."
    - "Changing only adjustment metadata changes derivative cache keys and produces new fingerprinted `/media/*` URLs after publish."
    - "Public static artifacts still exclude private originals, Object Storage identifiers, original filenames, unpublished media, and private adjustment JSON."
  artifacts:
    - path: "controller/src/publisher.rs"
      provides: "adjustment-aware derivative cache keys and adjusted derivative generation"
      contains: "adjustment"
    - path: "controller/tests/publisher.rs"
      provides: "regressions for adjustment-only derivative regeneration and media fingerprint changes"
    - path: "controller/tests/static_contract.rs"
      provides: "privacy regression for private adjustment metadata"
  key_links:
    - from: "controller/src/publisher.rs"
      to: "controller/src/image_adjustments.rs"
      via: "canonical adjustment cache key"
      pattern: "canonical_cache_key"
    - from: "controller/src/publisher.rs"
      to: "controller/src/derivatives.rs"
      via: "generate_adjusted_derivative"
      pattern: "generate_adjusted_derivative"
---

<objective>
Wire saved image adjustments into static publishing and derivative cache invalidation per D-08-18 and D-08-32.

Purpose: Public output must reflect saved adjustments only through normal publish, with new fingerprinted media paths and unchanged privacy guarantees.
Output: Adjustment-aware publisher cache keys, adjusted public derivative generation, static privacy tests, and runbook updates for MEDIA-05 and MEDIA-06.
Execution precondition: `08-02-SUMMARY.md` must record the landed 08-01/08-02 pre-media posture/security PR URL and merge commit SHA per D-08-14, and `08-06-SUMMARY.md` must record private preview/adjustment API and UI completion, before this publisher/cache plan starts.
</objective>

<execution_context>
@/home/jgreenwa/.codex/gsd-core/workflows/execute-plan.md
@/home/jgreenwa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-CONTEXT.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-RESEARCH.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-PATTERNS.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-02-SUMMARY.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-04-SUMMARY.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-06-SUMMARY.md
@.planning/phases/06-admin-collection-workflow/06-09-SUMMARY.md
@controller/src/publisher.rs
@controller/src/derivatives.rs
@controller/src/image_adjustments.rs
@controller/tests/publisher.rs
@controller/tests/static_contract.rs
@controller/tests/live_static_publish_smoke.rs
@docs/static-artifact-contract.md
@docs/static-runtime-runbook.md
</context>

## Artifacts This Phase Produces

- New enum variant or field shape: `DerivativeSourceKey::AdjustmentAware`
- New helper function: `adjusted_derivative_cache_key`
- New publish progress field: `adjustment_cache_key_count`
- New tests: `publisher_regenerates_derivatives_when_adjustment_changes_without_source_change`, `publisher_fingerprints_adjusted_derivative_paths`, `static_contract_excludes_private_adjustment_metadata`
- New live smoke assertions for adjustment publish path and changed `/media/*` fingerprint

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add adjustment-aware derivative cache keys</name>
  <files>controller/src/publisher.rs, controller/tests/publisher.rs</files>
  <read_first>
    @controller/src/publisher.rs
    @controller/src/image_adjustments.rs
    @controller/tests/publisher.rs
    @.planning/phases/08-admin-media-review-and-operational-posture/08-RESEARCH.md
    @.planning/phases/08-admin-media-review-and-operational-posture/08-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: two images with identical checksum and different adjustment metadata produce different derivative cache keys.
    - Test 2: an image with no adjustment preserves the existing checksum-backed cache behavior except for adding the transform version component required by the plan.
    - Test 3: metadata-only incremental publish reads source bytes when adjustment changes and does not reuse the stale unadjusted derivative cache entry.
  </behavior>
  <action>Per D-08-18 and D-08-32, update `derivative_cache_source_key(image: &AutographImage)` so the cache key includes source checksum, `IMAGE_ADJUSTMENT_TRANSFORM_VERSION`, and `image.adjustment.as_ref().map(ImageAdjustment::canonical_cache_key).unwrap_or("adjustment:none")`. Use a new helper `adjusted_derivative_cache_key(image: &AutographImage, checksum: &str) -> String` and track `adjustment_cache_key_count` in `PublishProgress` when the key includes adjustment metadata. Keep the public URL fingerprint based on final derivative bytes from `public_derivative_fingerprint`.</action>
  <acceptance_criteria>
    - Source assertion: `controller/src/publisher.rs` contains `IMAGE_ADJUSTMENT_TRANSFORM_VERSION`, `canonical_cache_key`, and `adjusted_derivative_cache_key`.
    - Behavior assertion: publisher tests fail before the cache key change and pass after it.
    - Behavior assertion: adjustment-only changes cannot reuse a stale derivative cache entry.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path controller/Cargo.toml --test publisher publisher_regenerates_derivatives_when_adjustment_changes_without_source_change -- --nocapture</automated>
    <automated>cargo test --manifest-path controller/Cargo.toml --test publisher -- --nocapture</automated>
  </verify>
  <done>Derivative cache keys change when adjustment metadata changes, even when original bytes and checksum stay the same.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate public derivatives from adjusted bytes and protect public privacy</name>
  <files>controller/src/publisher.rs, controller/tests/publisher.rs, controller/tests/static_contract.rs, docs/static-artifact-contract.md</files>
  <read_first>
    @controller/src/publisher.rs
    @controller/src/derivatives.rs
    @controller/src/image_adjustments.rs
    @controller/tests/publisher.rs
    @controller/tests/static_contract.rs
    @docs/static-artifact-contract.md
    @.planning/phases/08-admin-media-review-and-operational-posture/08-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: a saved rotation/crop/perspective adjustment changes public derivative dimensions or bytes compared with the unadjusted source.
    - Test 2: adjusted thumbnail and detail derivative paths include new byte fingerprints.
    - Test 3: generated public JSON and HTML do not contain `adjustment`, `adjustmentJson`, `object_key`, `originalFilename`, `bucket`, `namespace`, or unpublished media identifiers.
  </behavior>
  <action>Per D-08-32, replace publisher calls to `generate_derivative(bytes, variant)` with `generate_adjusted_derivative(bytes, variant, image.adjustment.as_ref())`. Add publisher tests proving adjusted derivative bytes and fingerprinted `/media/{slug}/image-{n}-{variant}-{fingerprint}.webp` paths change when only adjustment metadata changes. Extend static contract tests and `docs/static-artifact-contract.md` to state adjustment metadata is private admin metadata and must not appear in schema version 2 public collection/detail/facet JSON or HTML.</action>
  <acceptance_criteria>
    - Source assertion: `controller/src/publisher.rs` calls `generate_adjusted_derivative`.
    - Behavior assertion: `cargo test --manifest-path controller/Cargo.toml --test static_contract -- --nocapture` passes.
    - Source assertion: `docs/static-artifact-contract.md` contains `Image adjustment metadata is private admin metadata`.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path controller/Cargo.toml --test publisher publisher_fingerprints_adjusted_derivative_paths -- --nocapture</automated>
    <automated>cargo test --manifest-path controller/Cargo.toml --test static_contract -- --nocapture</automated>
  </verify>
  <done>Saved adjustments affect generated public derivatives and never leak private adjustment metadata into public artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Update live static publish smoke and run publisher gates</name>
  <files>controller/tests/live_static_publish_smoke.rs, docs/static-runtime-runbook.md, controller/tests/publisher.rs, controller/tests/static_contract.rs</files>
  <read_first>
    @controller/tests/live_static_publish_smoke.rs
    @docs/static-runtime-runbook.md
    @controller/tests/publisher.rs
    @controller/tests/static_contract.rs
    @.planning/phases/08-admin-media-review-and-operational-posture/08-CONTEXT.md
  </read_first>
  <action>Per D-08-31 and D-08-32, extend the ignored live static publish smoke so enabled runs save image adjustment metadata through the admin session route, publish normally, and assert the resulting public `/media/*` detail derivative URL changes after an adjustment-only save. Keep the smoke gated by existing live credentials and document the adjusted-media smoke steps in `docs/static-runtime-runbook.md` under `Phase 8 adjusted media publish verification`.</action>
  <acceptance_criteria>
    - Source assertion: `controller/tests/live_static_publish_smoke.rs` contains `Phase 8` or `adjustment` assertions and remains ignored/gated by `AUTOGRAPHS_LIVE_STATIC_PUBLISH_SMOKE`.
    - Source assertion: `docs/static-runtime-runbook.md` contains `Phase 8 adjusted media publish verification`.
    - CLI assertion: local ignored live smoke command passes or safely skips without live credentials.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path controller/Cargo.toml --features live-persistence live_static_publish_smoke -- --ignored --nocapture</automated>
    <automated>cargo test --manifest-path controller/Cargo.toml --test publisher -- --nocapture</automated>
    <automated>cargo test --manifest-path controller/Cargo.toml --test static_contract -- --nocapture</automated>
    <automated>cargo check --manifest-path controller/Cargo.toml --features production-persistence</automated>
  </verify>
  <done>The publish path applies adjustments, invalidates derivative cache entries, changes public media fingerprints, and preserves live-smoke/runbook coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Private catalog/media -> public static release | Saved adjustment metadata and private originals influence public derivatives. |
| Derivative cache -> public release | Cached bytes may be reused across publishes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-01 | Tampering | derivative cache | mitigate | Include canonical adjustment metadata and transform version in cache key. |
| T-08-07-02 | Information Disclosure | public JSON/HTML/media paths | mitigate | Static contract tests deny private adjustment fields, storage identifiers, original filenames, and unpublished media. |
| T-08-07-03 | Repudiation | publish output after adjustment | mitigate | Live smoke/runbook asserts adjustment-only publish changes public media fingerprint. |
</threat_model>

<verification>
Run task-level commands plus `cargo fmt --manifest-path controller/Cargo.toml --check`, `cargo test --manifest-path controller/Cargo.toml`, `cargo clippy --manifest-path controller/Cargo.toml --all-targets -- -D warnings`, and `git diff --check`.
</verification>

<success_criteria>
MEDIA-06 publishing scope is complete when saved adjustments are applied before resize/encode, adjustment metadata changes cache keys, public media fingerprints change, and static privacy validation remains intact.
</success_criteria>

<output>
Create `.planning/phases/08-admin-media-review-and-operational-posture/08-07-SUMMARY.md` when done.
</output>
