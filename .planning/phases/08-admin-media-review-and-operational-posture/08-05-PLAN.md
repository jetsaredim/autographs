---
phase: 08-admin-media-review-and-operational-posture
plan: "05"
type: execute
wave: 5
depends_on:
  - "08-04"
files_modified:
  - controller/src/oracle_catalog.rs
  - controller/src/oracle_schema.rs
  - controller/db/schema.sql
  - controller/db/updates/08-01-image-adjustments.sql
  - controller/tests/admin_workflow.rs
autonomous: true
requirements:
  - MEDIA-06
must_haves:
  truths:
    - "Oracle schema and preflight checks include the private `adjustment_json` column on `autograph_images`."
    - "Oracle persistence reads, writes, resets, and clears image adjustment metadata through the Plan 08-04 repository contract."
    - "Replacement of a private original clears saved adjustment metadata so stale corrections cannot carry across new media."
  artifacts:
    - path: "controller/db/updates/08-01-image-adjustments.sql"
      provides: "idempotent Oracle adjustment_json migration"
      contains: "adjustment_json"
    - path: "controller/src/oracle_catalog.rs"
      provides: "Oracle read/write/reset implementation for `CatalogRepository::update_image_adjustment`"
      contains: "update_image_adjustment"
    - path: "controller/src/oracle_schema.rs"
      provides: "schema preflight coverage for private adjustment metadata"
      contains: "adjustment_json"
  key_links:
    - from: "controller/src/oracle_catalog.rs"
      to: "controller/src/image_adjustments.rs"
      via: "typed JSON serialization and validation"
      pattern: "ImageAdjustment::(from_json|to_json)"
    - from: "controller/src/oracle_schema.rs"
      to: "controller/db/schema.sql"
      via: "preflight required column check"
      pattern: "adjustment_json"
---

<objective>
Persist non-destructive image adjustment metadata in Oracle per D-08-25, D-08-29, D-08-31, and D-08-32.

Purpose: The live catalog source of truth must carry the same private adjustment state as the validated catalog contract from Plan 08-04.
Output: Additive Oracle schema support, schema preflight coverage, Oracle repository save/reset behavior, and production-persistence validation for MEDIA-06.
Execution precondition: `08-04-SUMMARY.md` must record the committed adjustment DTOs, `AutographImage.adjustment`, `ImageAdjustmentChanged`, and `CatalogRepository::update_image_adjustment` contract before this plan starts.
</objective>

<execution_context>
@/home/jgreenwa/.codex/gsd-core/workflows/execute-plan.md
@/home/jgreenwa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-CONTEXT.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-RESEARCH.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-PATTERNS.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-04-SUMMARY.md
@controller/src/catalog.rs
@controller/src/image_adjustments.rs
@controller/src/oracle_catalog.rs
@controller/src/oracle_schema.rs
@controller/db/schema.sql
@controller/db/updates/06-03-media-cleanup.sql
@controller/tests/admin_workflow.rs
</context>

## Artifacts This Phase Produces

- New Oracle column: `autograph_images.adjustment_json`
- New migration file: `controller/db/updates/08-01-image-adjustments.sql`
- Updated Oracle select/insert/update mapping for `AutographImage.adjustment`
- Updated schema preflight required column check for `adjustment_json`
- Oracle implementation of `CatalogRepository::update_image_adjustment`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Oracle schema and preflight support for adjustment metadata</name>
  <files>controller/src/oracle_schema.rs, controller/db/schema.sql, controller/db/updates/08-01-image-adjustments.sql</files>
  <read_first>
    @controller/src/oracle_schema.rs
    @controller/db/schema.sql
    @controller/db/updates/06-03-media-cleanup.sql
    @.planning/phases/08-admin-media-review-and-operational-posture/08-RESEARCH.md
    @.planning/phases/08-admin-media-review-and-operational-posture/08-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: schema preflight treats `autograph_images.adjustment_json` as a required Phase 8 column.
    - Test 2: the end-state schema declares `adjustment_json clob` on `autograph_images`.
    - Test 3: the update migration is idempotent when `adjustment_json` already exists.
  </behavior>
  <action>Per D-08-25 and the resolved research storage shape, add `adjustment_json clob` to `controller/db/schema.sql` on `autograph_images`. Create `controller/db/updates/08-01-image-adjustments.sql` using the existing idempotent Oracle update pattern from `06-03-media-cleanup.sql` so live databases add the column only when absent. Update `controller/src/oracle_schema.rs` so production schema preflight requires `adjustment_json` for image adjustment readiness.</action>
  <acceptance_criteria>
    - Source assertion: `controller/db/schema.sql` contains `adjustment_json clob`.
    - Source assertion: `controller/db/updates/08-01-image-adjustments.sql` contains `adjustment_json` and an absence guard.
    - Source assertion: `controller/src/oracle_schema.rs` contains `adjustment_json`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "adjustment_json" controller/db/schema.sql controller/db/updates/08-01-image-adjustments.sql controller/src/oracle_schema.rs</automated>
  </verify>
  <done>Oracle end-state schema, live update migration, and schema preflight all require the private adjustment metadata column.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Oracle save, reset, read, and replacement semantics</name>
  <files>controller/src/oracle_catalog.rs, controller/tests/admin_workflow.rs</files>
  <read_first>
    @controller/src/oracle_catalog.rs
    @controller/src/catalog.rs
    @controller/src/image_adjustments.rs
    @controller/tests/admin_workflow.rs
    @.planning/phases/08-admin-media-review-and-operational-posture/08-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: Oracle row mapping reads `adjustment_json` into `AutographImage.adjustment` with `ImageAdjustment::from_json`.
    - Test 2: `update_image_adjustment(..., Some(adjustment))` writes typed private JSON, updates item timestamp, and records `ImageAdjustmentChanged`.
    - Test 3: `update_image_adjustment(..., None)` writes NULL and records `ImageAdjustmentChanged`.
    - Test 4: image replacement writes `adjustment_json` as NULL for the new private original.
  </behavior>
  <action>Per D-08-29 and D-08-31, add `adjustment_json` to `IMAGE_SELECT_COLUMNS`, `image_from_row`, image insert/update statements, and replacement handling in `controller/src/oracle_catalog.rs`. Serialize saved metadata with `ImageAdjustment::to_json`, parse selected values with `ImageAdjustment::from_json`, reject invalid persisted JSON with the existing redacted adapter error style, and write NULL on reset and replacement. Implement `CatalogRepository::update_image_adjustment` for Oracle with the same item timestamp, pending-change, and `ImageAdjustmentChanged` event semantics as the memory repository from Plan 08-04.</action>
  <acceptance_criteria>
    - Source assertion: `controller/src/oracle_catalog.rs` `IMAGE_SELECT_COLUMNS` includes `adjustment_json`.
    - Source assertion: `controller/src/oracle_catalog.rs` contains `ImageAdjustment::from_json`, `ImageAdjustment::to_json`, and `ImageAdjustmentChanged`.
    - Behavior assertion: `controller/tests/admin_workflow.rs` covers save/reset history and replacement clearing behavior without requiring live Oracle credentials.
    - CLI assertion: `cargo check --manifest-path controller/Cargo.toml --features production-persistence` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path controller/Cargo.toml --test admin_workflow -- --nocapture</automated>
    <automated>cargo check --manifest-path controller/Cargo.toml --features production-persistence</automated>
  </verify>
  <done>Oracle reads, writes, resets, and clears adjustment metadata through the private catalog adapter with production-persistence compilation proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller -> Oracle | Private adjustment metadata crosses from validated Rust DTOs into production persistence. |
| Oracle -> controller | Persisted JSON is untrusted when read back into the domain model. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Tampering | `adjustment_json` persistence | mitigate | Serialize through typed `ImageAdjustment::to_json` and validate with `from_json` on read. |
| T-08-05-02 | Information Disclosure | Oracle adjustment metadata | mitigate | Keep `adjustment_json` private to Oracle/controller and expose public privacy checks in Plan 08-07. |
| T-08-05-03 | Repudiation | image adjustment persistence events | mitigate | Record `ImageAdjustmentChanged` edit-history events on save and reset. |
</threat_model>

<verification>
Run task-level automated checks plus `cargo fmt --manifest-path controller/Cargo.toml --check` and `git diff --check`.
</verification>

<success_criteria>
MEDIA-06 Oracle persistence is complete when adjustment metadata exists in schema/preflight, reads and writes through the Oracle adapter, reset stores NULL, replacement clears stale metadata, and production-persistence compile checks pass.
</success_criteria>

<output>
Create `.planning/phases/08-admin-media-review-and-operational-posture/08-05-SUMMARY.md` when done.
</output>
