---
phase: 08-admin-media-review-and-operational-posture
plan: "08"
type: execute
wave: 8
depends_on:
  - "08-07"
files_modified:
  - docs/security-patching.md
  - docs/cdn-cache-contract.md
  - docs/dns-runbook.md
  - docs/static-runtime-runbook.md
  - docs/deployment-runbook.md
  - docs/security-review.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/TESTING.md
  - .planning/phases/08-admin-media-review-and-operational-posture/08-SOURCE-COVERAGE.md
autonomous: false
requirements:
  - OPS-01
  - OPS-02
  - MEDIA-05
  - MEDIA-06
  - ADMIN-06
user_setup:
  - service: cloudflare
    why: "Production CDN/cache enablement and verification after adjusted-media cache behavior is proven"
    env_vars: []
    dashboard_config:
      - task: "Proxy the production hostname through Cloudflare with Full (strict) TLS or the account's equivalent strict origin TLS setting."
        location: "Cloudflare dashboard -> Websites -> selected zone -> DNS and SSL/TLS"
      - task: "Create cache rule `Bypass admin and API` for `/admin*` and `/admin/api/*` with cache bypass."
        location: "Cloudflare dashboard -> Rules -> Cache Rules"
      - task: "Create cache rule `Respect rollback-sensitive public documents` for public HTML/JSON paths with origin-respecting or 60-second freshness."
        location: "Cloudflare dashboard -> Rules -> Cache Rules"
      - task: "Create cache rule `Cache fingerprinted media and assets` for `/media/*` and `/assets/*` while respecting origin cache headers."
        location: "Cloudflare dashboard -> Rules -> Cache Rules"
must_haves:
  truths:
    - "Live/operator verification instructions cover repaired security scan, dry-run apply, private previews, adjusted publish, and CDN cache behavior."
    - "Production CDN enablement happens only after adjusted-media cache behavior is proven."
    - "Phase 8 source coverage records GOAL, REQ, RESEARCH, and CONTEXT coverage with D-08-01 through D-08-34 cited."
  artifacts:
    - path: ".planning/phases/08-admin-media-review-and-operational-posture/08-SOURCE-COVERAGE.md"
      provides: "multi-source coverage audit for Phase 8"
      contains: "D-08-34"
    - path: "docs/security-review.md"
      provides: "Phase 8 ASVS L1 security review"
      contains: "Phase 8 Admin Media Review and Operational Posture Security Review"
    - path: "docs/cdn-cache-contract.md"
      provides: "post-media CDN verification evidence checklist"
      contains: "CF-Cache-Status"
  key_links:
    - from: "docs/cdn-cache-contract.md"
      to: "docs/static-runtime-runbook.md"
      via: "adjusted media publish proof before CDN enablement"
      pattern: "adjusted-media cache behavior"
    - from: "docs/security-patching.md"
      to: "deploy/ansible/roles/security_patching"
      via: "operator scan/apply verification"
      pattern: "Phase 8 scanner repair verification"
---

<objective>
Close Phase 8 with security, docs, source coverage, and production CDN verification gates after adjusted-media cache behavior exists per D-08-01, D-08-02, D-08-11, D-08-17, D-08-19, D-08-31, and D-08-32.

Purpose: Prove the operational and media-review work as an end-to-end owner workflow rather than leaving it as isolated local changes.
Output: Phase 8 security review, source coverage audit, closeout updates to architecture/concerns/testing maps, operator verification docs, and a blocking CDN verification checkpoint.
Execution precondition: `08-02-SUMMARY.md` must record the landed 08-01/08-02 pre-media posture/security PR URL and merge commit SHA per D-08-14, and `08-07-SUMMARY.md` must record adjusted-media publish/cache evidence, before this closeout plan starts.
</objective>

<execution_context>
@/home/jgreenwa/.codex/gsd-core/workflows/execute-plan.md
@/home/jgreenwa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-CONTEXT.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-RESEARCH.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-PATTERNS.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-UI-SPEC.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-02-SUMMARY.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-07-SUMMARY.md
@docs/security-patching.md
@docs/cdn-cache-contract.md
@docs/static-runtime-runbook.md
@docs/security-review.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
</context>

## Artifacts This Phase Produces

- New file path: `.planning/phases/08-admin-media-review-and-operational-posture/08-SOURCE-COVERAGE.md`
- New documentation section: `Phase 8 Admin Media Review and Operational Posture Security Review`
- New documentation section: `Phase 8 production verification`
- New coverage tables: `Goal coverage`, `Requirement coverage`, `Research coverage`, `Decision coverage`
- New checkpoint evidence fields: `security_scan_run_id`, `dry_run_apply_run_id`, `adjusted_media_publish_evidence`, `cdn_admin_bypass_evidence`, `cdn_media_cache_evidence`, `rollback_evidence`

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 8 security review, source coverage, and codebase map closeout</name>
  <files>docs/security-review.md, .planning/phases/08-admin-media-review-and-operational-posture/08-SOURCE-COVERAGE.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/CONCERNS.md, .planning/codebase/TESTING.md</files>
  <read_first>
    @.planning/phases/08-admin-media-review-and-operational-posture/08-CONTEXT.md
    @.planning/phases/08-admin-media-review-and-operational-posture/08-RESEARCH.md
    @.planning/phases/08-admin-media-review-and-operational-posture/08-PATTERNS.md
    @.planning/phases/08-admin-media-review-and-operational-posture/08-UI-SPEC.md
    @docs/security-review.md
    @.planning/codebase/ARCHITECTURE.md
    @.planning/codebase/CONCERNS.md
    @.planning/codebase/TESTING.md
  </read_first>
  <action>Add `Phase 8 Admin Media Review and Operational Posture Security Review` to `docs/security-review.md` with ASVS L1 sections for admin preview auth, no-store/CDN bypass, adjustment validation, private original preservation, public static privacy, derivative cache integrity, security patch issue approval/drift checks, and Cloudflare operational configuration. Every finding must have one of `Fixed`, `Accepted`, or `Operator verification required`; there must be no `HIGH: unmitigated` text. Create `.planning/phases/08-admin-media-review-and-operational-posture/08-SOURCE-COVERAGE.md` with GOAL, REQ, RESEARCH, and CONTEXT tables; cite every D-08-01 through D-08-34 and map them to plans 08-01 through 08-08. Refresh only `.planning/codebase/ARCHITECTURE.md`, `.planning/codebase/CONCERNS.md`, and `.planning/codebase/TESTING.md` for closeout evidence, marking Phase 8 implemented after execution and identifying Phase 9/10 as later roadmap phases without moving their work into Phase 8. Do not modify controller, deploy, workflow, Terraform, or static asset implementation files in this closeout plan.</action>
  <acceptance_criteria>
    - Source assertion: `docs/security-review.md` contains `Phase 8 Admin Media Review and Operational Posture Security Review`, `ASVS L1`, and no `HIGH: unmitigated`.
    - Source assertion: `08-SOURCE-COVERAGE.md` contains `D-08-01` and `D-08-34`.
    - Source assertion: all five requirement IDs `MEDIA-05`, `MEDIA-06`, `ADMIN-06`, `OPS-01`, and `OPS-02` appear in the source coverage file.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Phase 8 Admin Media Review and Operational Posture Security Review|ASVS L1" docs/security-review.md</automated>
    <automated>! rg -n "HIGH: unmitigated" docs/security-review.md</automated>
    <automated>rg -n "D-08-01|D-08-34|MEDIA-05|MEDIA-06|ADMIN-06|OPS-01|OPS-02" .planning/phases/08-admin-media-review-and-operational-posture/08-SOURCE-COVERAGE.md</automated>
  </verify>
  <done>Phase 8 has a committed ASVS L1 review, source coverage audit, and closeout map updates for completed operational/media surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Document and run final local verification gates</name>
  <files>docs/security-patching.md, docs/cdn-cache-contract.md, docs/static-runtime-runbook.md, docs/deployment-runbook.md</files>
  <read_first>
    @docs/security-patching.md
    @docs/cdn-cache-contract.md
    @docs/static-runtime-runbook.md
    @docs/deployment-runbook.md
    @.planning/phases/08-admin-media-review-and-operational-posture/08-CONTEXT.md
  </read_first>
  <action>Update the listed docs with a `Phase 8 production verification` section that records the local command set and the required live evidence fields: `security_scan_run_id`, `dry_run_apply_run_id`, `adjusted_media_publish_evidence`, `cdn_admin_bypass_evidence`, `cdn_media_cache_evidence`, and `rollback_evidence`. Run local checks for Rust, static admin JS, Ansible syntax/lint, repo hygiene, Caddy cache tests, publisher tests, static contract tests, and the ignored live static publish smoke command that safely skips without credentials.</action>
  <acceptance_criteria>
    - Source assertion: docs contain all six required evidence field names.
    - CLI assertion: Rust formatting, tests, clippy, production-persistence check, static admin JS syntax, Ansible syntax/lint, repo hygiene, Caddy cache tests, publisher tests, and static contract tests pass or document only credential-gated live skips.
    - Source assertion: docs state production CDN enablement follows adjusted-media publish evidence.
  </acceptance_criteria>
  <verify>
    <automated>cargo fmt --manifest-path controller/Cargo.toml --check</automated>
    <automated>cargo test --manifest-path controller/Cargo.toml</automated>
    <automated>cargo clippy --manifest-path controller/Cargo.toml --all-targets -- -D warnings</automated>
    <automated>cargo check --manifest-path controller/Cargo.toml --features production-persistence</automated>
    <automated>node --check controller/static-admin/admin.js</automated>
    <automated>python3 scripts/validate_repo_hygiene.py</automated>
    <automated>ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_CONFIG=deploy/ansible/ansible.cfg ansible-playbook --syntax-check deploy/ansible/playbooks/deploy.yml deploy/ansible/playbooks/system-cleanup.yml deploy/ansible/playbooks/security-scan.yml deploy/ansible/playbooks/security-patch.yml deploy/ansible/playbooks/security-patch-cleanup.yml</automated>
    <automated>ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_CONFIG=deploy/ansible/ansible.cfg ansible-lint deploy/ansible/</automated>
    <automated>cargo test --manifest-path controller/Cargo.toml --features live-persistence live_static_publish_smoke -- --ignored --nocapture</automated>
  </verify>
  <done>Local verification evidence is documented and all non-live gates pass before production CDN verification.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Enable and verify production CDN after adjusted-media proof</name>
  <files>docs/cdn-cache-contract.md, docs/dns-runbook.md, docs/static-runtime-runbook.md, docs/deployment-runbook.md</files>
  <read_first>
    @docs/cdn-cache-contract.md
    @docs/dns-runbook.md
    @docs/static-runtime-runbook.md
    @docs/deployment-runbook.md
    @.planning/phases/08-admin-media-review-and-operational-posture/08-CONTEXT.md
  </read_first>
  <action>After Task 2 records adjusted-media publish evidence, configure Cloudflare with the four `user_setup` dashboard tasks in this plan. Then verify D-08-02 and D-08-19 by capturing headers for `/admin/`, `/admin/api/status`, `/data/collection.json`, and a current `/media/*-{fingerprint}.webp` path. Admin and admin API responses must show `Cache-Control: no-store` and must not show `CF-Cache-Status: HIT`; repeated media requests should show Cloudflare cache participation such as `CF-Cache-Status: HIT`, `MISS`, `REVALIDATED`, or the account-equivalent cache header while preserving origin `Cache-Control`. Record the concrete command outputs in the plan summary and update the four listed docs if the live account behavior uses different header names.</action>
  <acceptance_criteria>
    - Human-check assertion: Cloudflare proxy/CDN is enabled for the production hostname after adjusted-media publish evidence exists.
    - Human-check assertion: `/admin/` and `/admin/api/status` are not cached by Cloudflare and retain `Cache-Control: no-store`.
    - Human-check assertion: a fingerprinted `/media/*` derivative participates in CDN caching and the adjusted publish produced a changed media URL.
    - Human-check assertion: rollback behavior is tested or documented with the exact reason it could not be safely executed.
  </acceptance_criteria>
  <verify>
    <automated>AUTOGRAPHS_PUBLIC_HOST=example.com bash -lc 'printf "%s\n" "Replace example.com with the production host, then run: curl -I https://$AUTOGRAPHS_PUBLIC_HOST/admin/ ; curl -I https://$AUTOGRAPHS_PUBLIC_HOST/admin/api/status ; curl -I https://$AUTOGRAPHS_PUBLIC_HOST/data/collection.json ; curl -I https://$AUTOGRAPHS_PUBLIC_HOST/media/REPLACE-WITH-FINGERPRINTED-DERIVATIVE.webp ; curl -I https://$AUTOGRAPHS_PUBLIC_HOST/media/REPLACE-WITH-FINGERPRINTED-DERIVATIVE.webp"'</automated>
    <human-check>Configure Cloudflare rules and paste the verification output into the execution summary, or describe the exact blocker.</human-check>
  </verify>
  <done>Production CDN is enabled and verified after adjusted-media cache behavior is proven, with admin/API bypass, rollback-friendly HTML/JSON, and fingerprinted media caching documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production browser -> Cloudflare -> Caddy | Live CDN rules affect public and admin request confidentiality/freshness. |
| Operator -> GitHub Actions/OCI runtime | Live verification may trigger scans and dry-run apply behavior against production infrastructure. |
| Static release rollback -> CDN cache | Rollback depends on HTML/JSON freshness and media fingerprint behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-01 | Information Disclosure | Cloudflare admin/API cache | mitigate | Blocking production verification requires admin/API bypass and `no-store` evidence. |
| T-08-08-02 | Tampering | adjusted-media CDN cache | mitigate | Enable CDN only after adjusted-media publish proves new fingerprinted paths. |
| T-08-08-03 | Elevation of Privilege | production patch apply | mitigate | Docs and verification preserve approver allowlist, label gate, and package drift checks. |
| T-08-08-04 | Repudiation | phase closeout evidence | mitigate | Source coverage and docs require concrete evidence fields and summary outputs. |
</threat_model>

<verification>
Complete task-level automated checks and the blocking Cloudflare verification checkpoint. Do not mark this plan complete until the checkpoint has either passed or recorded an exact external blocker.
</verification>

<success_criteria>
Phase 8 is ready for verification when source coverage, security review, local gates, live security scan/apply guidance, adjusted-media publish evidence, and production CDN cache behavior are documented with no unresolved high-severity threats.
</success_criteria>

<output>
Create `.planning/phases/08-admin-media-review-and-operational-posture/08-08-SUMMARY.md` when done.
</output>
