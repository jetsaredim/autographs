---
phase: 08-admin-media-review-and-operational-posture
plan: "04"
type: execute
wave: 4
depends_on:
  - "08-03"
files_modified:
  - controller/Cargo.toml
  - controller/Cargo.lock
  - controller/src/lib.rs
  - controller/src/image_adjustments.rs
  - controller/src/derivatives.rs
  - controller/src/catalog.rs
  - controller/tests/image_adjustments.rs
  - controller/tests/admin_workflow.rs
autonomous: true
requirements:
  - MEDIA-06
must_haves:
  truths:
    - "Image adjustment metadata can be validated, serialized canonically, saved in the memory repository, reset, and recorded in edit history."
    - "Private originals remain unchanged while adjusted derivative bytes can be generated from the same source bytes."
    - "The catalog model exposes the adjustment field and repository contract that Oracle persistence implements in Plan 08-05."
  artifacts:
    - path: "controller/src/image_adjustments.rs"
      provides: "Image adjustment DTOs, validation, canonical cache key, transform helper, and auto-assist proposal"
      exports: ["ImageAdjustment", "ImageCrop", "ImagePoint", "ImagePerspective", "ImageAdjustmentProposal", "IMAGE_ADJUSTMENT_TRANSFORM_VERSION"]
    - path: "controller/src/catalog.rs"
      provides: "AutographImage adjustment field, repository contract, memory repository save/reset, and edit-history event"
      contains: "ImageAdjustmentChanged"
    - path: "controller/tests/image_adjustments.rs"
      provides: "validation, canonicalization, transform, and auto-assist tests"
  key_links:
    - from: "controller/src/catalog.rs"
      to: "controller/src/image_adjustments.rs"
      via: "AutographImage.adjustment"
      pattern: "adjustment: Option<ImageAdjustment>"
    - from: "controller/src/derivatives.rs"
      to: "controller/src/image_adjustments.rs"
      via: "generate_adjusted_derivative"
      pattern: "generate_adjusted_derivative"
---

<objective>
Create the non-destructive image adjustment model, catalog contract, memory repository behavior, and transform foundation per D-08-25, D-08-26, D-08-29, D-08-31, and D-08-32.

Purpose: Give the backend a validated adjustment contract before UI, Oracle persistence, and publisher work depend on it.
Output: Rust adjustment types, catalog model/repository seam, memory repository save/reset behavior, derivative transform helper, and tests for MEDIA-06.
Execution precondition: `08-02-SUMMARY.md` must record the landed 08-01/08-02 pre-media posture/security PR URL and merge commit SHA per D-08-14 before this media foundation plan starts.
</objective>

<execution_context>
@/home/jgreenwa/.codex/gsd-core/workflows/execute-plan.md
@/home/jgreenwa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-CONTEXT.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-RESEARCH.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-PATTERNS.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-02-SUMMARY.md
@controller/Cargo.toml
@controller/src/lib.rs
@controller/src/catalog.rs
@controller/src/derivatives.rs
@controller/tests/admin_workflow.rs
@controller/tests/publisher.rs
</context>

## Artifacts This Phase Produces

- New crate dependency: `imageproc = "0.27.0"`
- New file path: `controller/src/image_adjustments.rs`
- New file path: `controller/tests/image_adjustments.rs`
- New module export: `pub mod image_adjustments`
- New constant: `IMAGE_ADJUSTMENT_TRANSFORM_VERSION`
- New structs/enums: `ImageAdjustment`, `ImageCrop`, `ImagePoint`, `ImagePerspective`, `ImageAdjustmentProposal`, `ImageAdjustmentProposalStatus`
- New functions/methods: `ImageAdjustment::identity`, `ImageAdjustment::validate`, `ImageAdjustment::canonical_cache_key`, `ImageAdjustment::is_identity`, `ImageAdjustment::from_json`, `ImageAdjustment::to_json`, `apply_image_adjustment`, `propose_image_adjustment`, `generate_adjusted_derivative`
- New field: `AutographImage.adjustment: Option<ImageAdjustment>`
- New enum variant: `EditEventKind::ImageAdjustmentChanged`
- New trait method: `CatalogRepository::update_image_adjustment`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add validated image adjustment types and transform helpers</name>
  <files>controller/Cargo.toml, controller/Cargo.lock, controller/src/lib.rs, controller/src/image_adjustments.rs, controller/src/derivatives.rs, controller/tests/image_adjustments.rs</files>
  <read_first>
    @.planning/phases/08-admin-media-review-and-operational-posture/08-CONTEXT.md
    @.planning/phases/08-admin-media-review-and-operational-posture/08-RESEARCH.md
    @.planning/phases/08-admin-media-review-and-operational-posture/08-PATTERNS.md
    @controller/Cargo.toml
    @controller/src/lib.rs
    @controller/src/derivatives.rs
    @controller/tests/publisher.rs
  </read_first>
  <behavior>
    - Test 1: default `ImageAdjustment::identity()` validates and serializes to a stable `adjustment:none` cache key.
    - Test 2: rotation outside `-15.0..=15.0`, zoom outside `1.0..=3.0`, pan outside `-1.0..=1.0`, crop outside normalized bounds, or perspective points outside `0.0..=1.0` are rejected.
    - Test 3: `generate_adjusted_derivative` returns WebP bytes with the requested variant and leaves `generate_derivative(source, variant)` behavior unchanged.
    - Test 4: `propose_image_adjustment` returns `status: "confident"` with exactly four normalized corners for a deterministic high-contrast skew fixture.
    - Test 5: `propose_image_adjustment` returns `status: "unavailable"` with the exact UI copy `Auto correction could not find reliable edges. Adjust the corners manually.` only for a separate low-confidence or ambiguous fixture.
  </behavior>
  <action>Per D-08-25 and D-08-26, add `imageproc = "0.27.0"` to `controller/Cargo.toml` and update `controller/Cargo.lock`. Create `controller/src/image_adjustments.rs` and export it from `controller/src/lib.rs`. Define `IMAGE_ADJUSTMENT_TRANSFORM_VERSION: &str = "image-adjustment-transform-01"`, `ImageAdjustment`, `ImageCrop`, `ImagePoint`, `ImagePerspective`, `ImageAdjustmentProposal`, and `ImageAdjustmentProposalStatus` with serde camelCase payloads. Implement bounded validation for rotation degrees, zoom, pan, crop, and four perspective corners. Implement canonical JSON/cache-key serialization with deterministic field order through typed structs. Add `apply_image_adjustment` using `imageproc::geometric_transformations` for small rotation and perspective warp, then crop/zoom/pan before resize. Implement `propose_image_adjustment` as a bounded deterministic heuristic: a high-contrast skew fixture must return `status: "confident"` with four normalized corners, while `status: "unavailable"` is reserved for separate low-confidence or ambiguous fixtures. In `controller/src/derivatives.rs`, add `generate_adjusted_derivative(source, variant, adjustment)` and keep `generate_derivative(source, variant)` as a wrapper that passes no adjustment.</action>
  <acceptance_criteria>
    - Source assertion: `controller/src/lib.rs` contains `pub mod image_adjustments;`.
    - Source assertion: `controller/src/image_adjustments.rs` contains `IMAGE_ADJUSTMENT_TRANSFORM_VERSION`, `ImageAdjustment`, `ImagePerspective`, `canonical_cache_key`, `apply_image_adjustment`, and `propose_image_adjustment`.
    - Behavior assertion: `controller/tests/image_adjustments.rs` contains separate confident high-contrast and unavailable low-confidence auto-assist tests.
    - Behavior assertion: existing publisher tests using `generate_derivative` still compile without call-site changes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path controller/Cargo.toml --test image_adjustments -- --nocapture</automated>
    <automated>cargo test --manifest-path controller/Cargo.toml --test publisher -- --nocapture</automated>
  </verify>
  <done>The adjustment transform foundation exists, validates bounded metadata, and does not break existing derivative generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the catalog adjustment field, repository contract, and memory behavior</name>
  <files>controller/src/catalog.rs, controller/tests/admin_workflow.rs</files>
  <read_first>
    @controller/src/catalog.rs
    @controller/tests/admin_workflow.rs
    @controller/src/image_adjustments.rs
    @.planning/phases/08-admin-media-review-and-operational-posture/08-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: `MemoryCatalogRepository::update_image_adjustment(item_id, image_id, Some(adjustment))` updates the image, item timestamp, pending-change count, and history event.
    - Test 2: `update_image_adjustment(..., None)` clears saved metadata and records an `ImageAdjustmentChanged` event.
    - Test 3: replacing an image preserves the admin-facing image ID and clears old adjustment metadata for the new private original.
  </behavior>
  <action>Per D-08-29 and D-08-31, add `adjustment: Option<ImageAdjustment>` to `AutographImage`, add `ImageAdjustmentChanged` to `EditEventKind`, and add `CatalogRepository::update_image_adjustment(&self, item_id: Uuid, image_id: Uuid, adjustment: Option<ImageAdjustment>) -> Result<AutographItem, String>`. Implement it for `MemoryCatalogRepository` with validation, `updated_at_epoch_seconds`, a summary `Image adjustments changed`, and pending-change/history behavior. Replacement metadata must set the replacement image's adjustment to `None` so adjustments do not carry across new originals. Plan 08-05 implements the Oracle adapter, schema preflight, and live migration against this repository contract.</action>
  <acceptance_criteria>
    - Source assertion: `controller/src/catalog.rs` contains `adjustment: Option<ImageAdjustment>`, `ImageAdjustmentChanged`, and `update_image_adjustment`.
    - Behavior assertion: `controller/tests/admin_workflow.rs` covers save and reset history for `EditEventKind::ImageAdjustmentChanged`.
    - Behavior assertion: replacement clears stale adjustment metadata for the new private original while preserving the admin-facing image ID.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path controller/Cargo.toml --test admin_workflow -- --nocapture</automated>
  </verify>
  <done>The catalog model and memory repository support save/reset adjustment metadata, replacement clears stale adjustments, and edit history records the change.</done>
</task>

<task type="auto">
  <name>Task 3: Run adjustment foundation quality gates</name>
  <files>controller/Cargo.toml, controller/Cargo.lock, controller/src/image_adjustments.rs, controller/src/derivatives.rs, controller/src/catalog.rs, controller/tests/image_adjustments.rs, controller/tests/admin_workflow.rs</files>
  <read_first>
    @controller/Cargo.toml
    @controller/src/image_adjustments.rs
    @controller/src/derivatives.rs
    @controller/src/catalog.rs
    @controller/tests/image_adjustments.rs
    @controller/tests/admin_workflow.rs
    @.planning/phases/08-admin-media-review-and-operational-posture/08-RESEARCH.md
  </read_first>
  <action>Per D-08-32, verify the package legitimacy audit from `08-RESEARCH.md` before keeping `imageproc`. Run formatting, targeted transform/model tests, full controller tests, and clippy. If clippy reports pre-existing warnings outside this plan's files, record them in the summary without modifying unrelated files. Do not change public publisher cache behavior in this plan; Plan 08-07 owns adjustment-aware publisher cache behavior.</action>
  <acceptance_criteria>
    - CLI assertion: `cargo fmt --manifest-path controller/Cargo.toml --check` passes.
    - CLI assertion: `cargo test --manifest-path controller/Cargo.toml --test image_adjustments -- --nocapture` passes.
    - Source assertion: no public static JSON/HTML contract files are modified in this plan.
  </acceptance_criteria>
  <verify>
    <automated>cargo fmt --manifest-path controller/Cargo.toml --check</automated>
    <automated>cargo test --manifest-path controller/Cargo.toml</automated>
    <automated>cargo clippy --manifest-path controller/Cargo.toml --all-targets -- -D warnings</automated>
  </verify>
  <done>The adjustment transform and catalog foundation passes local Rust quality gates and is ready for Oracle persistence, admin API/UI, and publisher integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin API -> repository | Untrusted adjustment payloads become catalog metadata through the repository contract. |
| Private media bytes -> transform pipeline | Uploaded image bytes are decoded and transformed before preview/public derivative output. |
| Cargo registry -> build | New `imageproc` dependency enters the Rust build graph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering | `ImageAdjustment` payload | mitigate | Validate bounded normalized geometry and reject invalid projections before persistence. |
| T-08-04-02 | Denial of Service | image transform pipeline | mitigate | Preserve 20 MiB source limit and bounded transform dimensions through existing derivative path. |
| T-08-04-03 | Information Disclosure | adjustment DTO exposure | mitigate | Keep adjustment metadata in private catalog structures and require Plan 08-07 public contract tests before publisher exposure. |
| T-08-04-SC | Tampering | Cargo dependency install | mitigate | `08-RESEARCH.md` Package Legitimacy Audit marks `imageproc` as OK; keep Cargo.lock review and run cargo tests/clippy. |
</threat_model>

<verification>
Run all task-level automated checks and `git diff --check`.
</verification>

<success_criteria>
MEDIA-06 transform/domain foundation is complete when adjustment metadata validates, saves and resets in the memory repository, records history, and can produce adjusted derivative bytes without mutating private originals.
</success_criteria>

<output>
Create `.planning/phases/08-admin-media-review-and-operational-posture/08-04-SUMMARY.md` when done.
</output>
