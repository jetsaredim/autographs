---
phase: 08-admin-media-review-and-operational-posture
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/weekly-security-scan.yml
  - deploy/ansible/playbooks/security-scan.yml
  - deploy/ansible/roles/security_patching/defaults/main.yml
  - deploy/ansible/roles/security_patching/tasks/scan.yml
  - deploy/ansible/roles/security_patching/tasks/create_issue.yml
  - deploy/ansible/roles/security_patching/templates/security-report.md.j2
  - scripts/oracle_linux_advisory_enrichment.py
  - scripts/test_oracle_linux_advisory_enrichment.py
  - docs/security-patching.md
autonomous: true
requirements:
  - OPS-01
must_haves:
  truths:
    - "D-08-07: Production security scans create or update a scanner issue when package inventory exists, even if advisory enrichment fails."
    - "D-08-08: The hidden scanner issue metadata contains exact package specs only; advisory IDs, CVEs, severity, summaries, and links remain human-readable report detail only."
    - "The apply path still refuses drifted package sets before applying updates."
  artifacts:
    - path: "deploy/ansible/roles/security_patching/tasks/scan.yml"
      provides: "minimal runtime package inventory without the per-advisory dnf detail loop"
      contains: "security_patching_update_package_specs"
    - path: "scripts/oracle_linux_advisory_enrichment.py"
      provides: "best-effort Oracle Linux advisory enrichment outside the runtime host loop"
    - path: "deploy/ansible/roles/security_patching/templates/security-report.md.j2"
      provides: "GitHub issue report with package-spec metadata and concise advisory detail"
  key_links:
    - from: "deploy/ansible/playbooks/security-scan.yml"
      to: "deploy/ansible/roles/security_patching/tasks/create_issue.yml"
      via: "localhost issue rendering after runtime host scan"
      pattern: "tasks_from: create_issue"
    - from: "deploy/ansible/roles/security_patching/tasks/create_issue.yml"
      to: "scripts/oracle_linux_advisory_enrichment.py"
      via: "best-effort localhost command"
      pattern: "oracle_linux_advisory_enrichment.py"
---

<objective>
Repair the production security patching scanner so Phase 8 starts by fixing the current post-IP-resolution Ansible scan failure per D-08-01 and D-08-04.

Purpose: The scan path must collect the exact package inventory needed for approval and drift checks without timing out in a slow host-side advisory-detail loop.
Output: Minimal runtime scan facts, external best-effort Oracle advisory enrichment, scanner issue rendering, and updated operator guidance for OPS-01.
</objective>

<execution_context>
@/home/jgreenwa/.codex/gsd-core/workflows/execute-plan.md
@/home/jgreenwa/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-CONTEXT.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-RESEARCH.md
@.planning/phases/08-admin-media-review-and-operational-posture/08-PATTERNS.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@docs/security-patching.md
@deploy/ansible/playbooks/security-scan.yml
@deploy/ansible/playbooks/security-patch.yml
@deploy/ansible/roles/security_patching/tasks/scan.yml
@deploy/ansible/roles/security_patching/tasks/create_issue.yml
@deploy/ansible/roles/security_patching/templates/security-report.md.j2
</context>

## Artifacts This Phase Produces

- New file path: `scripts/oracle_linux_advisory_enrichment.py`
- New file path: `scripts/test_oracle_linux_advisory_enrichment.py`
- New Ansible facts: `security_patching_enrichment_status`, `security_patching_enrichment_message`, `security_patching_enrichment_by_host`
- New Python CLI flags: `--input`, `--output`, `--oval-url`, `--errata-base-url`
- New Python functions: `parse_update_entries`, `load_oval_enrichment`, `build_errata_link`, `enrich_inventory`, `main`
- New report states: `complete`, `degraded`, `minimal`

<tasks>

<task type="auto">
  <name>Task 1: Replace the slow runtime advisory loop with minimal package inventory</name>
  <files>.github/workflows/weekly-security-scan.yml, deploy/ansible/playbooks/security-scan.yml, deploy/ansible/roles/security_patching/defaults/main.yml, deploy/ansible/roles/security_patching/tasks/scan.yml</files>
  <read_first>
    @.planning/phases/08-admin-media-review-and-operational-posture/08-CONTEXT.md
    @.planning/phases/08-admin-media-review-and-operational-posture/08-RESEARCH.md
    @.planning/phases/08-admin-media-review-and-operational-posture/08-PATTERNS.md
    @.github/workflows/weekly-security-scan.yml
    @deploy/ansible/playbooks/security-scan.yml
    @deploy/ansible/roles/security_patching/defaults/main.yml
    @deploy/ansible/roles/security_patching/tasks/scan.yml
  </read_first>
  <action>Per D-08-04 and D-08-05, keep `dnf -q updateinfo list --security --available` as the host inventory source, remove the `Read advisory details` task that loops over `dnf -q updateinfo info "{{ item }}"`, and set `security_patching_update_package_specs` directly from parsed `package_spec` values in `security_patching_entries_raw`. Preserve `security_patching_update_advisory_ids` and `security_patching_update_entries`, but initialize each entry with `cves: []` and `enrichmentStatus: "minimal"` until localhost enrichment runs. Add defaults `security_patching_enrichment_status: minimal` and `security_patching_enrichment_message: "Runtime package inventory was collected without host-side advisory detail enrichment."`. If `gh run list --workflow weekly-security-scan.yml --limit 5` is available, record the latest failing run ID and failure step in the summary; do not block the code repair if GitHub auth is unavailable.</action>
  <acceptance_criteria>
    - Source assertion: `deploy/ansible/roles/security_patching/tasks/scan.yml` does not contain `updateinfo\n      - info`.
    - Source assertion: `deploy/ansible/roles/security_patching/tasks/scan.yml` still contains `updateinfo`, `list`, `--security`, `--available`, and `security_patching_update_package_specs`.
    - Behavior assertion: a scan with parsed `security_patching_entries_raw` produces sorted unique package specs without reading advisory details on the runtime host.
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' deploy/ansible/roles/security_patching/tasks/scan.yml | grep -c 'updateinfo info' | grep '^0$'</automated>
    <automated>ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_CONFIG=deploy/ansible/ansible.cfg ansible-playbook --syntax-check deploy/ansible/playbooks/security-scan.yml deploy/ansible/playbooks/security-patch.yml deploy/ansible/playbooks/security-patch-cleanup.yml</automated>
  </verify>
  <done>The runtime scan path no longer performs per-advisory `dnf updateinfo info` calls, and it still emits exact package specs for scanner approval and drift checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add best-effort Oracle advisory enrichment outside the host loop</name>
  <files>deploy/ansible/roles/security_patching/tasks/create_issue.yml, deploy/ansible/roles/security_patching/templates/security-report.md.j2, scripts/oracle_linux_advisory_enrichment.py, scripts/test_oracle_linux_advisory_enrichment.py</files>
  <read_first>
    @.planning/phases/08-admin-media-review-and-operational-posture/08-CONTEXT.md
    @.planning/phases/08-admin-media-review-and-operational-posture/08-RESEARCH.md
    @deploy/ansible/roles/security_patching/tasks/create_issue.yml
    @deploy/ansible/roles/security_patching/templates/security-report.md.j2
    @scripts/test_release_version.py
  </read_first>
  <behavior>
    - Test 1: Given host update entries with `ELSA-2025-20632`, `parse_update_entries` and `build_errata_link` produce `https://linux.oracle.com/errata/ELSA-2025-20632.html`.
    - Test 2: Given OVAL XML containing a CVE for an advisory, `load_oval_enrichment` returns the CVE and severity without copying advisory bodies.
    - Test 3: Given an unavailable OVAL source, `enrich_inventory` returns `status: degraded`, preserves package specs, and still emits errata links.
  </behavior>
  <action>Per D-08-06 through D-08-09, create `scripts/oracle_linux_advisory_enrichment.py` as a small Python standard-library CLI that reads inventory JSON from `--input`, writes enrichment JSON to `--output`, fetches Oracle Linux OVAL from `--oval-url` when reachable, and falls back to Oracle errata links from `--errata-base-url https://linux.oracle.com/errata/`. In `create_issue.yml`, write a localhost inventory JSON file containing each host's `security_patching_update_entries`, run the script with `changed_when: false` and `failed_when: false`, load the output when valid, and otherwise set `security_patching_enrichment_status: degraded` plus a concise `security_patching_enrichment_message`. Update `security-report.md.j2` so the hidden metadata block still contains only `package_specs`, while visible rows show advisory links, severity, CVEs, and an enrichment status note.</action>
  <acceptance_criteria>
    - Source assertion: the hidden `autographs-security-patch-metadata` block in `security-report.md.j2` contains `package_specs` and does not contain `cves`, `severity`, `summary`, or `advisory`.
    - Negative assertion: forbidden advisory detail fields are checked only inside the hidden metadata block; visible human-readable report rows may still contain concise advisory IDs, CVEs, severity, summaries, and links per D-08-08 and D-08-09.
    - Behavior assertion: `scripts/test_oracle_linux_advisory_enrichment.py` covers successful OVAL enrichment and degraded fallback.
    - Behavior assertion: `create_issue.yml` uses `failed_when: false` for the enrichment command so a valid package inventory can still create or update an issue per D-08-07.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest scripts/test_oracle_linux_advisory_enrichment.py</automated>
    <automated>python3 scripts/oracle_linux_advisory_enrichment.py --input /tmp/nonexistent-security-input.json --output /tmp/nonexistent-security-output.json; test "$?" != "0"</automated>
    <automated>python3 -c "from pathlib import Path; text=Path('deploy/ansible/roles/security_patching/templates/security-report.md.j2').read_text(); start=text.index('autographs-security-patch-metadata'); end=text.index('-->', start); block=text[start:end]; assert 'package_specs' in block; forbidden=['cves','severity','summary','advisory']; leaked=[name for name in forbidden if name in block]; assert not leaked, leaked"</automated>
  </verify>
  <done>Scanner issues preserve package-spec-only approval metadata and add concise authoritative advisory detail when enrichment succeeds or a degraded note when it does not.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve stale approval removal and document scanner verification</name>
  <files>deploy/ansible/roles/security_patching/tasks/create_issue.yml, docs/security-patching.md</files>
  <read_first>
    @.planning/phases/08-admin-media-review-and-operational-posture/08-CONTEXT.md
    @deploy/ansible/roles/security_patching/tasks/create_issue.yml
    @deploy/ansible/roles/security_patching/tasks/validate_request.yml
    @deploy/ansible/roles/security_patching/tasks/cleanup_failed_request.yml
    @docs/security-patching.md
  </read_first>
  <action>Per D-08-10 through D-08-12, ensure updating an existing open scanner issue replaces the full report body and labels with `security_patching_issue_labels` only, so `approved-production-update` is removed when the package set changes. Update `docs/security-patching.md` with an operator verification section named `Phase 8 scanner repair verification` containing the exact commands for `workflow_dispatch` scan proof, degraded enrichment review, same-issue update review, stale approval removal, and dry-run apply-path exercise. State that package row ordering is either DNF order or alphabetical package/version order after real output review.</action>
  <acceptance_criteria>
    - Source assertion: existing issue updates in `create_issue.yml` set `labels: "{{ security_patching_issue_labels }}"`.
    - Source assertion: `docs/security-patching.md` contains `Phase 8 scanner repair verification`, `workflow_dispatch`, `degraded`, `approved-production-update`, and `dry-run`.
    - Behavior assertion: the documentation tells the operator not to install production updates unless a real approved package set exists.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Phase 8 scanner repair verification|workflow_dispatch|degraded|approved-production-update|dry-run" docs/security-patching.md</automated>
    <automated>ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ANSIBLE_CONFIG=deploy/ansible/ansible.cfg ansible-lint deploy/ansible/roles/security_patching deploy/ansible/playbooks/security-scan.yml deploy/ansible/playbooks/security-patch.yml deploy/ansible/playbooks/security-patch-cleanup.yml</automated>
  </verify>
  <done>OPS-01 has a locally validated repair path and an operator-run proof path for live scan, degraded enrichment, stale approval removal, and dry-run apply behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions -> runtime VM | Scheduled/manual workflows invoke Ansible on the production host. |
| Runtime VM -> GitHub issue | Host inventory becomes a browser-visible GitHub issue report. |
| External Oracle/NVD sources -> issue renderer | Advisory enrichment is untrusted report detail and must not become approval metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Denial of Service | `scan.yml` host advisory loop | mitigate | Remove per-advisory `dnf updateinfo info` host loop and create issues from minimal package inventory. |
| T-08-01-02 | Tampering | security patch approval metadata | mitigate | Keep hidden metadata to exact package specs only; preserve validate_request drift checks. |
| T-08-01-03 | Repudiation | scanner issue update | mitigate | Update the existing issue body and labels on package drift and document scan IDs in the report. |
| T-08-01-04 | Information Disclosure | report template | mitigate | Render concise CVE/advisory links only; do not copy advisory bodies or host internals. |
</threat_model>

<verification>
Run the task-level automated checks, then run `git diff --check`. If GitHub access is available, include the latest failed weekly scan ID and the first successful post-repair manual scan ID in the plan summary.
</verification>

<success_criteria>
OPS-01 is satisfied for local planning execution when the scan role no longer loops over `dnf updateinfo info`, issue creation survives enrichment failure with package inventory intact, package specs remain the approval contract, and operator verification instructions are committed.
</success_criteria>

<output>
Create `.planning/phases/08-admin-media-review-and-operational-posture/08-01-SUMMARY.md` when done.
</output>
