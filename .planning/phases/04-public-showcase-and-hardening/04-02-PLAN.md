---
phase: 04-public-showcase-and-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - renovate.json
  - docs/dependency-updates.md
  - .github/workflows/ci.yml
  - .github/workflows/deploy.yml
  - .github/workflows/data-smoke.yml
  - .github/workflows/image-cleanup.yml
autonomous: true
requirements:
  - SHIP-01
  - SHIP-02
  - SHIP-05
must_haves:
  truths:
    - "Renovate is configured as the dependency automation tool per D-04-09."
    - "Package, workflow, Docker/container, Terraform, and custom maintained surfaces are covered conservatively per D-04-10."
    - "Dependency update review expectations are documented per D-04-11."
    - "Workflow permissions and third-party actions have an explicit review note."
  artifacts:
    - path: "renovate.json"
      provides: "Dependency automation configuration"
    - path: "docs/dependency-updates.md"
      provides: "Dependency review and supply-chain guidance"
    - path: ".github/workflows/ci.yml"
      provides: "CI permission and action surface"
    - path: ".github/workflows/deploy.yml"
      provides: "Deploy permission and action surface"
  key_links:
    - from: "renovate.json"
      to: "package.json, app/package.json, .github/workflows, Dockerfile, infra/terraform"
      via: "Renovate managers/package rules"
---

<objective>
Add conservative dependency automation and supply-chain hygiene for the maintained repository surfaces.

Purpose: Implement D-04-09 through D-04-11 and SHIP-02 while folding workflow permission review into SHIP-01.
Output: Renovate config plus dependency review documentation and workflow permission/action review notes.
</objective>

<execution_context>
@/home/jgreenwa/.codex/get-shit-done/workflows/execute-plan.md
@/home/jgreenwa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/04-public-showcase-and-hardening/04-CONTEXT.md
@.planning/phases/04-public-showcase-and-hardening/RESEARCH.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/STACK.md
@package.json
@app/package.json
@.github/workflows/ci.yml
@.github/workflows/deploy.yml
@.github/workflows/data-smoke.yml
@.github/workflows/image-cleanup.yml
@infra/terraform/versions.tf
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external-dependency-to-repo | Renovate proposes code/config changes from upstream package and action ecosystems. |
| workflow-to-cloud | GitHub Actions uses repo permissions and secrets to build, publish, deploy, and run smoke checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | dependency update PRs | mitigate | Configure conservative Renovate grouping, schedules, lockfile maintenance, and review docs; avoid automerge unless already justified. |
| T-04-06 | Elevation of Privilege | GitHub Actions permissions | mitigate | Review workflow `permissions` blocks and document least-privilege posture or tracked exceptions. |
| T-04-07 | Spoofing | third-party actions/images | mitigate | Inventory actions/container images and document review expectations for tags, digests, and trusted publishers. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; Renovate config only. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Configure Renovate</name>
  <files>renovate.json</files>
  <action>Create `renovate.json` per D-04-09 using conservative settings: weekly cadence, grouped non-major updates where useful, separate major updates, lockfile maintenance, and coverage for npm/pnpm, GitHub Actions, Docker/container references, Terraform providers/modules, and regex/custom maintained surfaces such as Caddy image defaults if Renovate cannot detect them automatically. Do not use Dependabot, because the user locked Renovate.</action>
  <verify><automated>test -s renovate.json && node -e "JSON.parse(require('fs').readFileSync('renovate.json','utf8'))"</automated></verify>
  <done>`renovate.json` is valid JSON and describes the maintained update surfaces without noisy automerge behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Review workflow permissions and action surfaces</name>
  <files>.github/workflows/ci.yml, .github/workflows/deploy.yml, .github/workflows/data-smoke.yml, .github/workflows/image-cleanup.yml, docs/dependency-updates.md</files>
  <action>Audit each workflow for `permissions`, secret use, third-party actions, and container/image references. Tighten permissions where safe for current behavior and record any accepted exception in `docs/dependency-updates.md`; do not change deploy semantics or require new cloud setup.</action>
  <verify><automated>rg -n "^permissions:|contents:|packages:|actions:" .github/workflows docs/dependency-updates.md</automated></verify>
  <done>Workflow permission posture is either least-privilege or explicitly documented with current-surface rationale.</done>
</task>

<task type="auto">
  <name>Task 3: Document dependency update policy</name>
  <files>docs/dependency-updates.md</files>
  <action>Document how Renovate PRs should be reviewed, which surfaces it covers, what validation commands apply, what requires manual smoke/deploy review, and how Phase 5/6 must update the policy when admin or AI dependencies are added. Include D-04-10 lifecycle rationale and D-04-11 conservative review guidance.</action>
  <verify><automated>test -s docs/dependency-updates.md && rg -n "Renovate|GitHub Actions|Terraform|Docker|manual smoke|Phase 5|Phase 6" docs/dependency-updates.md</automated></verify>
  <done>Dependency automation is understandable to a public reader and actionable for the solo operator.</done>
</task>

</tasks>

<verification>
Validate Renovate JSON with Node, run `corepack pnpm --filter app lint`, and run existing workflow validation through CI or local actionlint if available.
</verification>

<success_criteria>
SHIP-02 is covered by committed Renovate automation and SHIP-01/SHIP-05 supply-chain concerns are reviewed without introducing runtime package changes.
</success_criteria>

<output>
Create `.planning/phases/04-public-showcase-and-hardening/04-02-SUMMARY.md` when done.
</output>
