---
phase: 02-oracle-and-private-media-core
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - infra/terraform/
  - infra/terraform/modules/
  - infra/terraform/environments/prod/terraform.tfvars.example
  - docs/configuration-contract.md
  - .github/.env.github.example
  - .env.example
requirements:
  - DATA-01
  - DATA-02
  - MEDIA-01
  - MEDIA-03
autonomous: true
user_setup:
  - service: oci
    why: "ADB wallet material, database password handling, and object-storage namespace/bucket values must be supplied through local or GitHub secret channels, not committed files."
must_haves:
  truths:
    - "Terraform describes the Phase 2 ADB and private Object Storage resources needed by the app."
    - "The app/runtime configuration contract includes database and private media settings without committing secret material."
    - "The runtime has least-privilege access seams for private bucket reads/writes and Oracle connectivity."
  artifacts:
    - path: "infra/terraform/"
      provides: "ADB, media bucket, IAM, and outputs for Phase 2 resources"
    - path: "docs/configuration-contract.md"
      provides: "Extended local/GitHub config contract for data and media"
    - path: ".env.example"
      provides: "Local placeholder environment variables"
    - path: ".github/.env.github.example"
      provides: "GitHub Secret/Variable inventory"
---

<objective>
Provision and document the OCI data services needed by Phase 2.

Purpose: Add Terraform and configuration seams for Oracle Autonomous Database Free and private Object Storage so the app can use real OCI data resources.
Output: Terraform resources/modules, outputs, example variables, and updated config contract docs.
</objective>

<execution_context>
@/home/jgreenwa/.codex/get-shit-done/workflows/execute-plan.md
@/home/jgreenwa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-oracle-and-private-media-core/02-CONTEXT.md
@.planning/phases/02-oracle-and-private-media-core/02-RESEARCH.md
@.planning/phases/02-oracle-and-private-media-core/02-01-SUMMARY.md
@infra/terraform/main.tf
@infra/terraform/variables.tf
@infra/terraform/outputs.tf
@docs/configuration-contract.md
@docs/deployment-runbook.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Terraform inputs and modules for ADB and private media bucket</name>
  <read_first>
    - infra/terraform/main.tf
    - infra/terraform/modules/iam/main.tf
    - infra/terraform/modules/state_bucket/main.tf
    - infra/terraform/environments/prod/terraform.tfvars.example
  </read_first>
  <files>
    infra/terraform/main.tf
    infra/terraform/variables.tf
    infra/terraform/outputs.tf
    infra/terraform/modules/
    infra/terraform/environments/prod/terraform.tfvars.example
  </files>
  <acceptance_criteria>
    - Terraform defines or cleanly composes resources for an ADB Free-compatible database and a private Object Storage bucket.
    - Terraform variables/examples expose required database, bucket, namespace, and naming values without secrets.
    - Terraform outputs include app-consumable bucket/database coordinates while keeping credentials out of output.
  </acceptance_criteria>
  <action>Extend the Phase 1 Terraform baseline with Phase 2 data resources. Preserve existing module style. Keep buckets private. Use sensible OCI Free Tier-compatible defaults where possible, but keep operator-specific values in tfvars examples rather than hardcoded production identifiers.</action>
  <verify>
    <automated>.tools/terraform/terraform -chdir=infra/terraform fmt -check</automated>
    <automated>.tools/terraform/terraform -chdir=infra/terraform validate</automated>
  </verify>
  <done>Terraform can validate with the new ADB/Object Storage resources and exposes the non-secret outputs the app/deploy flow needs.</done>
</task>

<task type="auto">
  <name>Task 2: Extend IAM and runtime access boundaries</name>
  <read_first>
    - infra/terraform/modules/iam/main.tf
    - deploy/compose/compose.prod.yaml
    - .github/workflows/deploy.yml
  </read_first>
  <files>
    infra/terraform/modules/iam/
    deploy/compose/compose.prod.yaml
    .github/workflows/deploy.yml
  </files>
  <acceptance_criteria>
    - IAM policies are scoped to the project compartment and required data/media operations.
    - Runtime deployment can receive bucket/database coordinates via environment variables.
    - No object-storage credentials or database secrets are committed.
  </acceptance_criteria>
  <action>Add least-privilege seams for the runtime/deploy identity to access the private media bucket and database metadata path. Wire non-secret runtime coordinates into deployment where appropriate, and leave secret values sourced from GitHub Secrets or local environment only.</action>
  <verify>
    <automated>rg -n "object|bucket|database|autonomous|policy|secret|variable" infra/terraform deploy/compose .github/workflows/deploy.yml</automated>
  </verify>
  <done>The runtime can be configured for private media/database access without broad tenancy permissions or committed credentials.</done>
</task>

<task type="auto">
  <name>Task 3: Update configuration docs and examples</name>
  <read_first>
    - docs/configuration-contract.md
    - .env.example
    - .github/.env.github.example
  </read_first>
  <files>
    docs/configuration-contract.md
    docs/deployment-runbook.md
    .env.example
    .github/.env.github.example
  </files>
  <acceptance_criteria>
    - Config docs classify database wallet/password/API credentials as secrets.
    - Config docs classify bucket namespace/name and non-sensitive resource coordinates as variables.
    - Deployment runbook explains the Phase 2 data-resource setup and live smoke expectations.
  </acceptance_criteria>
  <action>Extend the Phase 1 configuration contract for database and media. Make local, GitHub Actions, and deployed runtime expectations explicit. Include wallet handling, database user/password handling, bucket/namespace variables, and any required OCI SDK/API auth values.</action>
  <verify>
    <automated>rg -n "ADB|Autonomous|Object Storage|bucket|wallet|database|media" docs/configuration-contract.md docs/deployment-runbook.md .env.example .github/.env.github.example</automated>
  </verify>
  <done>Operators have a single documented source of truth for all Phase 2 data/media configuration.</done>
</task>

</tasks>

<verification>
Run Terraform format/validate and grep the config docs/examples for all Phase 2 settings. If live OCI apply is not available, document the exact GitHub/main deploy or local Terraform command needed for operator proof.
</verification>

<success_criteria>
OCI data resources and runtime configuration are represented in Terraform and docs with private bucket behavior, least-privilege access seams, and no committed secrets.
</success_criteria>

<output>
After completion, create `.planning/phases/02-oracle-and-private-media-core/02-02-SUMMARY.md`.
</output>
