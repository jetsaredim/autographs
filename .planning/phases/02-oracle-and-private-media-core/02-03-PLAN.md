---
phase: 02-oracle-and-private-media-core
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - app/src/catalog/
  - app/src/media/
  - app/app/api/
  - app/app/health/route.ts
requirements:
  - DATA-02
  - MEDIA-01
  - MEDIA-02
autonomous: true
must_haves:
  truths:
    - "The app can create, read, update, and list autograph records through route/service boundaries."
    - "The app can attach multiple private images to one autograph item with one primary image."
    - "Mutation surfaces remain safe until Phase 4 adds the final admin authentication workflow."
  artifacts:
    - path: "app/src/media/"
      provides: "Private Object Storage upload/read adapter"
    - path: "app/app/api/"
      provides: "Thin route handlers for Phase 2 verification"
    - path: "app/src/catalog/"
      provides: "Combined catalog and media metadata operations"
---

<objective>
Implement the application service and API seams over Oracle metadata and private Object Storage media.

Purpose: Prove the app can manage records and private multi-image attachments through reusable boundaries before gallery/admin UI work starts.
Output: Media adapter, route handlers or server actions for verification, and combined catalog/media operations.
</objective>

<execution_context>
@/home/jgreenwa/.codex/get-shit-done/workflows/execute-plan.md
@/home/jgreenwa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-oracle-and-private-media-core/02-CONTEXT.md
@.planning/phases/02-oracle-and-private-media-core/02-RESEARCH.md
@.planning/phases/02-oracle-and-private-media-core/02-01-SUMMARY.md
@.planning/phases/02-oracle-and-private-media-core/02-02-SUMMARY.md
@app/app/health/route.ts
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the private Object Storage adapter</name>
  <read_first>
    - .planning/phases/02-oracle-and-private-media-core/02-CONTEXT.md
    - docs/configuration-contract.md
  </read_first>
  <files>
    app/src/media/
    app/package.json
  </files>
  <acceptance_criteria>
    - Adapter can upload object bytes with content type and metadata.
    - Adapter can read object bytes/streams by app-owned identifiers without returning public URLs.
    - Adapter has a local/test mode or clear failure behavior when live OCI credentials are unavailable.
  </acceptance_criteria>
  <action>Add a private media adapter around OCI Object Storage access. Keep object keys and credentials server-side. Prefer streaming reads/writes where practical and avoid loading large image objects into memory unless unavoidable for the chosen SDK.</action>
  <verify>
    <automated>corepack pnpm --filter app typecheck</automated>
  </verify>
  <done>The app has a server-only private media adapter for upload and read operations.</done>
</task>

<task type="auto">
  <name>Task 2: Combine catalog records with image metadata operations</name>
  <read_first>
    - app/src/catalog/
    - app/src/media/
    - app/db/migrations/
  </read_first>
  <files>
    app/src/catalog/
    app/src/media/
  </files>
  <acceptance_criteria>
    - Service can attach multiple images to a record and designate exactly one primary image.
    - Service persists object metadata in Oracle after successful storage writes.
    - Service can read one item with ordered images and list items with primary-image metadata.
  </acceptance_criteria>
  <action>Implement the cross-resource operations that bind database rows to private object storage. Keep failure handling conservative: do not persist metadata that points to a failed upload, and document any cleanup gap deferred to Phase 4 orphan handling.</action>
  <verify>
    <automated>corepack pnpm --filter app typecheck</automated>
  </verify>
  <done>Catalog operations can manage the relationship between autograph records and their private images.</done>
</task>

<task type="auto">
  <name>Task 3: Add verification route handlers without creating public admin APIs</name>
  <read_first>
    - app/app/health/route.ts
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>
    app/app/api/
    app/src/catalog/
  </files>
  <acceptance_criteria>
    - Read/list route handlers expose only safe published/sample data behavior needed for Phase 2 verification.
    - Mutation route handlers are guarded, local-only, script-driven, or otherwise clearly not a public unauthenticated admin surface.
    - Route handlers reuse the service layer rather than duplicating SQL/storage logic.
  </acceptance_criteria>
  <action>Add the thinnest app-facing verification layer needed to prove create/read/update/list and media attachment behavior. Do not accidentally launch the final admin API before Phase 4 auth exists; if mutation endpoints are necessary, guard them with an explicit operator/local secret or keep them script-only.</action>
  <verify>
    <automated>corepack pnpm --filter app lint</automated>
    <automated>corepack pnpm --filter app typecheck</automated>
  </verify>
  <done>The Phase 2 behavior is callable through safe app boundaries and ready for downstream gallery/admin features.</done>
</task>

<task type="auto">
  <name>Task 4: Extend health/smoke checks for data-service readiness</name>
  <read_first>
    - scripts/validate-ci.sh
    - app/app/health/route.ts
  </read_first>
  <files>
    app/app/health/route.ts
    scripts/validate-ci.sh
    scripts/validate-runtime.sh
  </files>
  <acceptance_criteria>
    - Existing proof-of-life health checks keep working without production secrets.
    - A separate data/media smoke command can verify live Oracle/Object Storage when credentials are present.
    - CI remains green in environments without live OCI data-service access.
  </acceptance_criteria>
  <action>Add optional data/media readiness checks without making `/health` fail in CI or local development when Phase 2 secrets are absent. Keep proof-of-life health stable and add an explicit deeper smoke path for live verification.</action>
  <verify>
    <automated>bash scripts/validate-ci.sh</automated>
  </verify>
  <done>The repo can distinguish basic app health from live data/media integration health.</done>
</task>

</tasks>

<verification>
Run lint/typecheck and the shared CI validation script. If live OCI credentials are available, run the data/media smoke route or script and record the command/output in the plan summary.
</verification>

<success_criteria>
The app can manage autograph metadata and private image attachments through safe reusable service/API seams, without exposing public mutation surfaces or storage credentials.
</success_criteria>

<output>
After completion, create `.planning/phases/02-oracle-and-private-media-core/02-03-SUMMARY.md`.
</output>
