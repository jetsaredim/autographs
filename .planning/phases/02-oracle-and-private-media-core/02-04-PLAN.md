---
phase: 02-oracle-and-private-media-core
plan: 04
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - app/db/seed/
  - app/scripts/
  - app/app/api/
  - docs/
  - scripts/
requirements:
  - DATA-04
  - MEDIA-02
  - MEDIA-03
autonomous: true
must_haves:
  truths:
    - "Representative records and images can be loaded for local/operator verification."
    - "Public users can view images through app-mediated delivery without direct object URLs."
    - "Phase 2 has a documented live smoke path proving Oracle plus private Object Storage together."
  artifacts:
    - path: "app/db/seed/"
      provides: "Representative metadata and media fixture definitions"
    - path: "app/app/api/"
      provides: "App-mediated private media read route"
    - path: "docs/"
      provides: "Operator/developer instructions for Phase 2 data/media verification"
    - path: "scripts/"
      provides: "Smoke command for live data/media proof"
---

<objective>
Finish Phase 2 by proving sample data and app-mediated private image delivery end to end.

Purpose: Satisfy `DATA-04`, `MEDIA-02`, and `MEDIA-03` with a verification path later gallery work can trust.
Output: Seed fixtures, private image read route, smoke script/runbook, and Phase 2 completion evidence.
</objective>

<execution_context>
@/home/jgreenwa/.codex/get-shit-done/workflows/execute-plan.md
@/home/jgreenwa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-oracle-and-private-media-core/02-CONTEXT.md
@.planning/phases/02-oracle-and-private-media-core/02-RESEARCH.md
@.planning/phases/02-oracle-and-private-media-core/02-01-SUMMARY.md
@.planning/phases/02-oracle-and-private-media-core/02-02-SUMMARY.md
@.planning/phases/02-oracle-and-private-media-core/02-03-SUMMARY.md
@docs/deployment-runbook.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize representative seed records and image fixtures</name>
  <read_first>
    - app/db/seed/
    - app/scripts/
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>
    app/db/seed/
    app/scripts/
  </files>
  <acceptance_criteria>
    - Seed path creates multiple autograph records with complete required metadata coverage.
    - Seed path attaches at least one primary image and one supporting image for a representative item.
    - Seed path is repeatable or clearly documents reset/idempotency expectations.
  </acceptance_criteria>
  <action>Round out the seed flow so it proves both the record shape and media relationship model. Use fixture images that are safe to commit or generate tiny placeholder images at seed time; do not commit private personal collection assets.</action>
  <verify>
    <automated>rg -n "primary|supporting|publication|certification|inscription|event" app/db/seed app/scripts</automated>
  </verify>
  <done>Representative sample data can exercise the metadata and image relationship model without relying on private assets.</done>
</task>

<task type="auto">
  <name>Task 2: Implement app-mediated private image read route</name>
  <read_first>
    - app/src/media/
    - app/src/catalog/
    - .planning/phases/02-oracle-and-private-media-core/02-CONTEXT.md
  </read_first>
  <files>
    app/app/api/
    app/src/media/
    app/src/catalog/
  </files>
  <acceptance_criteria>
    - Route resolves an image through catalog/media metadata and reads from private Object Storage server-side.
    - Response includes appropriate content type and cache headers for app-mediated image delivery.
    - Route does not reveal Object Storage bucket credentials, signed direct URLs, or raw internal object identifiers as the public access contract.
  </acceptance_criteria>
  <action>Add the route that later gallery pages can use for image display. Keep authorization/publish filtering aligned with Phase 2: public read behavior should only serve images that are safe for published/sample records, while admin-only edge cases can wait for Phase 4.</action>
  <verify>
    <automated>corepack pnpm --filter app lint</automated>
    <automated>corepack pnpm --filter app typecheck</automated>
  </verify>
  <done>Image display happens through the app, with private Object Storage remaining behind the server boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Add live data/media smoke script and docs</name>
  <read_first>
    - scripts/validate-ci.sh
    - docs/deployment-runbook.md
    - docs/configuration-contract.md
  </read_first>
  <files>
    scripts/
    docs/
  </files>
  <acceptance_criteria>
    - Smoke script or documented command can prove migration, seed, record read/list, object upload, and app-mediated image read.
    - Smoke path clearly states required live OCI/ADB/Object Storage environment variables.
    - CI validation remains possible without production secrets.
  </acceptance_criteria>
  <action>Create the Phase 2 equivalent of the Phase 1 proof: one command path that verifies Oracle and private media together when credentials are present. Keep it optional for CI, but make it easy for the operator to run locally or through the deployed environment.</action>
  <verify>
    <automated>bash scripts/validate-ci.sh</automated>
    <automated>rg -n "Oracle|ADB|Object Storage|seed|media|smoke|private" docs scripts</automated>
  </verify>
  <done>Phase 2 has an explicit verification story instead of relying on undocumented manual clicking or ad hoc SQL/object checks.</done>
</task>

<task type="auto">
  <name>Task 4: Update planning status and downstream readiness notes</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/REQUIREMENTS.md
    .planning/phases/02-oracle-and-private-media-core/02-04-SUMMARY.md
  </files>
  <acceptance_criteria>
    - Phase 2 requirements completed by the implementation are marked complete only after verification evidence exists.
    - State points next to Phase 3 Public Gallery MVP if Phase 2 success criteria are met.
    - Summary captures exact verification commands and any operator-only live checks not run locally.
  </acceptance_criteria>
  <action>Close the loop on Phase 2 only after the smoke path proves the data/media core. Record what Phase 3 can assume: published records can be listed, full item metadata can be read, and images can be rendered through an app-mediated URL.</action>
  <verify>
    <automated>rg -n "Phase 2|Oracle and Private Media Core|DATA-01|MEDIA-03|Phase 3" .planning/ROADMAP.md .planning/STATE.md .planning/REQUIREMENTS.md .planning/phases/02-oracle-and-private-media-core/02-04-SUMMARY.md</automated>
  </verify>
  <done>Phase 2 is either formally complete with evidence or clearly documented with the remaining live operator gate.</done>
</task>

</tasks>

<verification>
Run lint, typecheck, shared CI validation, and the Phase 2 live smoke path when credentials are available. Verify that media reads happen through the app route and do not expose public Object Storage URLs.
</verification>

<success_criteria>
Phase 2 is complete when representative metadata and private images can be loaded, read, and displayed through app-mediated routes backed by Oracle and private OCI Object Storage.
</success_criteria>

<output>
After completion, create `.planning/phases/02-oracle-and-private-media-core/02-04-SUMMARY.md`.
</output>
