---
phase: 02-oracle-and-private-media-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/package.json
  - app/db/migrations/
  - app/src/db/
  - app/src/catalog/
  - app/scripts/
  - .env.example
requirements:
  - DATA-01
  - DATA-02
  - DATA-04
autonomous: true
must_haves:
  truths:
    - "The app has a typed Oracle-backed data model for autograph items and associated images."
    - "Database migrations can be applied repeatably without relying on manual SQL copy/paste."
    - "Local development can seed representative autograph metadata without production secrets."
  artifacts:
    - path: "app/db/migrations/"
      provides: "Versioned Oracle DDL for autograph metadata and image metadata tables"
    - path: "app/src/db/"
      provides: "Connection, migration, and query helpers"
    - path: "app/src/catalog/"
      provides: "Typed repository/service layer for autograph record CRUD and listing"
    - path: "app/scripts/"
      provides: "Scriptable migration and seed entrypoints"
  key_links:
    - from: "app/scripts/"
      to: "app/db/migrations/"
      via: "migration runner"
      pattern: "database migration"
    - from: "app/src/catalog/"
      to: "app/src/db/"
      via: "repository queries"
      pattern: "typed data layer"
---

<objective>
Add the Oracle metadata foundation for autograph records and media metadata.

Purpose: Satisfy the data-model portion of `DATA-01`, `DATA-02`, and `DATA-04` before storage and UI work depends on it.
Output: Oracle schema migrations, typed data access functions, and local seed/migration commands.
</objective>

<execution_context>
@/home/jgreenwa/.codex/get-shit-done/workflows/execute-plan.md
@/home/jgreenwa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-oracle-and-private-media-core/02-CONTEXT.md
@.planning/phases/02-oracle-and-private-media-core/02-RESEARCH.md
@docs/configuration-contract.md
@app/package.json
@app/tsconfig.json
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Choose and wire the Oracle client/migration dependency path</name>
  <read_first>
    - app/package.json
    - app/Dockerfile
    - scripts/validate-ci.sh
  </read_first>
  <files>
    app/package.json
    app/src/db/
    app/scripts/
    .env.example
  </files>
  <acceptance_criteria>
    - `corepack pnpm --filter app install --lockfile-only` or the repo-approved dependency update path captures any new dependencies in `pnpm-lock.yaml`.
    - `rg -n "DATABASE_URL|ORACLE|ADB|WALLET" .env.example app/src app/scripts` confirms the database config contract is explicit.
    - `corepack pnpm --filter app typecheck` passes after initial DB scaffolding.
  </acceptance_criteria>
  <action>Select the smallest Oracle-compatible Node.js data access path that works inside the existing Next.js container. Add connection/config helpers that read environment variables from the committed config contract without committing credentials. Keep the code organized so later tests can swap in a local/test adapter if live Oracle is unavailable.</action>
  <verify>
    <automated>corepack pnpm --filter app typecheck</automated>
  </verify>
  <done>The app has a compiled database module boundary and an explicit Oracle/ADB environment contract.</done>
</task>

<task type="auto">
  <name>Task 2: Create autograph and image metadata migrations</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/02-oracle-and-private-media-core/02-CONTEXT.md
  </read_first>
  <files>
    app/db/migrations/
    app/src/db/migrations.ts
  </files>
  <acceptance_criteria>
    - Migration files define autograph item fields required by `DATA-01`.
    - Migration files define associated image metadata required by `MEDIA-02`: primary flag, supporting images, order, object identifier, content type, and storage metadata.
    - Migration runner records applied migration versions and is safe to rerun.
  </acceptance_criteria>
  <action>Add versioned Oracle DDL for the v1 catalog backbone. Include publication status and timestamps now, but defer edit-history tables unless a lightweight compatibility seam is needed. Use relational tables for core item/image relationships and avoid storing private object URLs as public data.</action>
  <verify>
    <automated>rg -n "autograph|image|primary|publication|migration" app/db app/src/db</automated>
  </verify>
  <done>Schema migrations can create the core catalog and image metadata tables from a clean database.</done>
</task>

<task type="auto">
  <name>Task 3: Implement typed CRUD/list repository functions</name>
  <read_first>
    - app/db/migrations/
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>
    app/src/catalog/
    app/src/db/
  </files>
  <acceptance_criteria>
    - Data layer supports create, read, update, and list operations for autograph metadata.
    - Types include all required `DATA-01` fields and image metadata references needed by later media work.
    - List/read functions can filter out unpublished items for later public gallery consumption.
  </acceptance_criteria>
  <action>Build a small typed repository/service layer over the database helpers. Keep SQL centralized, parameterized, and shaped for later route handlers. Return domain objects rather than raw database rows so Phase 3 and Phase 4 can reuse the same boundary.</action>
  <verify>
    <automated>corepack pnpm --filter app typecheck</automated>
  </verify>
  <done>The app can express catalog CRUD/list behavior through a typed service layer without route handlers reaching into SQL directly.</done>
</task>

<task type="auto">
  <name>Task 4: Add representative metadata seed support</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - app/src/catalog/
  </read_first>
  <files>
    app/scripts/
    app/db/seed/
    app/package.json
  </files>
  <acceptance_criteria>
    - A script can load representative sample autograph metadata for local or operator verification.
    - Seed data includes multiple categories, tags, publication statuses, certification fields, event/source fields, and image metadata placeholders.
    - Seed command is documented in `app/package.json` scripts or an adjacent README/runbook.
  </acceptance_criteria>
  <action>Add a seed path that exercises the typed catalog layer rather than bypassing it with raw one-off SQL. Keep fixtures realistic enough for later gallery and admin UI development, but small enough to run repeatedly during local verification.</action>
  <verify>
    <automated>rg -n "seed|sample|autograph" app/package.json app/scripts app/db</automated>
  </verify>
  <done>Developers can populate the Oracle-backed catalog with representative records for Phase 2 and downstream feature work.</done>
</task>

</tasks>

<verification>
Run typecheck and any new migration/seed dry-run commands. If live Oracle is not available in the execution environment, verify the migration SQL, seed fixtures, and typed repository compile path, then document the exact live smoke command for operator execution.
</verification>

<success_criteria>
The app has a repeatable Oracle metadata foundation: schema migrations, typed catalog CRUD/list functions, and representative seed data covering the full v1 record shape.
</success_criteria>

<output>
After completion, create `.planning/phases/02-oracle-and-private-media-core/02-01-SUMMARY.md`.
</output>
