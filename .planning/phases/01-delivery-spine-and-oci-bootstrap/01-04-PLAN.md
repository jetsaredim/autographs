---
phase: 01-delivery-spine-and-oci-bootstrap
plan: 04
type: execute
wave: 4
depends_on:
  - 01-02
  - 01-03
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/deploy.yml
  - .github/actions/setup-runtime/action.yml
  - scripts/validate-ci.sh
  - scripts/deploy-vm.sh
  - docs/configuration-contract.md
  - docs/deployment-runbook.md
  - .env.example
  - .github/.env.github.example
autonomous: true
requirements:
  - PLAT-02
  - PLAT-03
user_setup:
  - service: github-actions
    why: "Repo-level GitHub Secrets and Variables must be populated before deploy automation can run; GitHub Environments are optional and additive."
    env_vars:
      - name: OCI_CLI_USER_OCID
        source: "OCI Console -> User details for the deploy identity"
      - name: OCI_TENANCY_OCID
        source: "OCI Console -> Tenancy details"
      - name: OCI_FINGERPRINT
        source: "OCI API signing key metadata"
      - name: OCI_PRIVATE_KEY_PEM
        source: "Generated API signing private key stored as a GitHub Secret"
      - name: OCI_REGION
        source: "OCI region hosting the Phase 1 resources"
      - name: OCI_HOME_REGION
        source: "OCI tenancy home region for region-sensitive resources"
      - name: OCI_COMPARTMENT_OCID
        source: "Terraform-managed or operator-selected project compartment"
      - name: VM_PUBLIC_IP
        source: "Output from Terraform apply for the runtime VM"
      - name: GHCR_IMAGE_REPOSITORY
        source: "Repo-managed GitHub Variable naming the published `ghcr.io` image path"
must_haves:
  truths:
    - "Operator can configure local development and GitHub delivery using a committed contract that separates secrets, variables, and example files."
    - "Pull requests run automated repository validation before merge."
    - "Merges to `main` have a committed deployment path that publishes a prebuilt app image to `ghcr.io`, then verifies proof-of-life through nginx on OCI."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Pull request validation workflow"
    - path: ".github/workflows/deploy.yml"
      provides: "Main-branch workflow that publishes the app image to `ghcr.io` and deploys it to the VM"
    - path: "docs/configuration-contract.md"
      provides: "Source-of-truth config split for local env, GitHub Secrets, and GitHub Variables"
    - path: "scripts/deploy-vm.sh"
      provides: "VM deployment entrypoint used by GitHub Actions"
    - path: "docs/deployment-runbook.md"
      provides: "Operator runbook for end-to-end deployment and smoke verification"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/validate-ci.sh"
      via: "workflow job invoking the shared validation script"
      pattern: "validate-ci"
    - from: ".github/workflows/deploy.yml"
      to: "scripts/deploy-vm.sh"
      via: "main-branch deployment job"
      pattern: "deploy-vm"
    - from: "docs/configuration-contract.md"
      to: ".env.example"
      via: "documented local env contract"
      pattern: "GitHub Secrets"
---

<objective>
Turn the app/runtime and OCI baseline into a real delivery spine by defining the config contract, GitHub Actions workflows, and operator runbook for proof-of-life deployment.

Purpose: Satisfy PLAT-02 and finish the Phase 1 exit gate from D-16 and D-17 with a concrete PR-validation and main-branch deploy path.
Output: Config contract docs, CI workflow, deploy workflow, shared scripts, and the runbook for deploying the nginx -> Next.js stack to OCI.
</objective>

<execution_context>
@/home/jgreenwa/.codex/get-shit-done/workflows/execute-plan.md
@/home/jgreenwa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-delivery-spine-and-oci-bootstrap/01-CONTEXT.md
@.planning/phases/01-delivery-spine-and-oci-bootstrap/01-RESEARCH.md
@.planning/phases/01-delivery-spine-and-oci-bootstrap/01-02-PLAN.md
@.planning/phases/01-delivery-spine-and-oci-bootstrap/01-03-PLAN.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the committed configuration and secret contract</name>
  <read_first>
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-CONTEXT.md
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-RESEARCH.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>
    docs/configuration-contract.md
    .env.example
    .github/.env.github.example
  </files>
  <acceptance_criteria>
    - `test -f docs/configuration-contract.md && test -f .env.example && test -f .github/.env.github.example` succeeds.
    - `rg -n "GitHub Secrets|GitHub Variables|repo-level|optional GitHub Environments|replaceable auth|OCI API signing keys" docs/configuration-contract.md` confirms the split and the future-auth seam from D-10 through D-12.
    - `rg -n "OCI_REGION|OCI_HOME_REGION|OCI_COMPARTMENT_OCID|VM_PUBLIC_IP|GHCR_IMAGE_REPOSITORY" .env.example .github/.env.github.example` confirms the explicit variable contract for local and GitHub use.
  </acceptance_criteria>
  <action>Create the source-of-truth configuration contract for Phase 1. Split values across committed examples, repo-level GitHub Secrets, and repo-level GitHub Variables exactly as required by D-10 through D-12: sensitive OCI signing credentials and SSH material in Secrets, non-sensitive deployment coordinates plus the `ghcr.io` image naming inputs in Variables/example files, and local development values in a committed `.env.example`. Document GitHub Environments as optional/additive only, not required for the baseline contract. Document the current API-key-based auth path as replaceable so execution can later swap to OIDC or another short-lived auth flow without redesigning the workflow inputs.</action>
  <verify>
    <automated>rg -n "GitHub Secrets|GitHub Variables|repo-level|optional GitHub Environments|replaceable|OCI_API|OCI_REGION|VM_PUBLIC_IP|GHCR_IMAGE_REPOSITORY" docs/configuration-contract.md .env.example .github/.env.github.example</automated>
  </verify>
  <done>The repository has one explicit, committed config contract covering local operation and GitHub automation, and it preserves the planned auth replacement seam.</done>
</task>

<task type="auto">
  <name>Task 2: Add PR validation and main-branch deployment workflows</name>
  <read_first>
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-RESEARCH.md
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-02-PLAN.md
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-03-PLAN.md
  </read_first>
  <files>
    .github/workflows/ci.yml
    .github/workflows/deploy.yml
    .github/actions/setup-runtime/action.yml
    scripts/validate-ci.sh
    scripts/deploy-vm.sh
  </files>
  <acceptance_criteria>
    - `test -f .github/workflows/ci.yml && test -f .github/workflows/deploy.yml && test -f scripts/deploy-vm.sh` succeeds.
    - `rg -n "pull_request|main|terraform|docker|ghcr.io|ssh|health" .github/workflows/ci.yml .github/workflows/deploy.yml scripts/validate-ci.sh scripts/deploy-vm.sh` confirms validation and the locked registry-backed deploy behavior.
    - `rg -n "ghcr.io|docker/login-action|docker/build-push-action|OCI_PRIVATE_KEY_PEM|OCI_FINGERPRINT|OCI_REGION|VM_PUBLIC_IP|GHCR_IMAGE_REPOSITORY" .github/workflows/deploy.yml scripts/deploy-vm.sh` confirms the workflow publishes the app image and the VM pulls that exact artifact.
  </acceptance_criteria>
  <action>Implement the GitHub delivery spine with the deploy artifact strategy locked for Phase 1: GitHub Actions builds the prebuilt app image, publishes it to `ghcr.io`, and the OCI VM pulls that image during deploy. The PR workflow should install runtime dependencies and run the repository validation contract, including app checks and Terraform validation. The `main` deploy workflow should use the signing-key model from D-10, publish the app image to `ghcr.io`, apply Terraform as needed, deploy the runtime to the single OCI VM over SSH, run `docker compose pull`/restart against the published image, and probe the nginx-fronted proof endpoint before success. Keep auth inputs parameterized so the workflow can later move from API keys to OIDC without rewriting the rest of the pipeline, but do not leave image publication strategy open to a host-build alternative in this phase.</action>
  <verify>
    <automated>bash scripts/validate-ci.sh</automated>
  </verify>
  <done>Pull requests and `main` have committed automation paths, and the deploy automation targets the real Phase 1 runtime and infrastructure artifacts rather than placeholders.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the operator deployment runbook and smoke path</name>
  <read_first>
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-CONTEXT.md
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-RESEARCH.md
    - docs/configuration-contract.md
  </read_first>
  <files>
    docs/deployment-runbook.md
  </files>
  <acceptance_criteria>
    - `test -f docs/deployment-runbook.md` succeeds.
    - `rg -n "proof-of-life|nginx|/health|GitHub Actions|terraform apply|docker compose pull|ghcr.io" docs/deployment-runbook.md` confirms the runbook covers the full delivery path and exit gate.
    - `rg -n "home region|OCI_REGION|OCI_HOME_REGION|repo-level GitHub Secrets|optional GitHub Environments" docs/deployment-runbook.md` confirms the runbook handles the explicit operator inputs and the baseline GitHub contract.
  </acceptance_criteria>
  <action>Write the Phase 1 operator runbook that ties the whole bootstrap sequence together: local validation, OCI bootstrap prerequisites, repo-level GitHub secret/variable setup, optional GitHub Environments, expected workflow triggers, deploy execution, and proof-of-life verification through nginx on the OCI VM. Make the locked delivery path explicit: GitHub Actions publishes the app image to `ghcr.io`, then the VM pulls and restarts that image during deploy. Also spell out the required operator-supplied OCI inputs such as `OCI_REGION`, `OCI_HOME_REGION`, compartment identifiers, and naming prefixes so no region or naming decisions remain implicit.</action>
  <verify>
    <automated>rg -n "proof-of-life|GitHub Actions|nginx|/health|ghcr.io|docker compose pull|OCI_REGION|OCI_HOME_REGION|repo-level GitHub Secrets|optional GitHub Environments" docs/deployment-runbook.md</automated>
  </verify>
  <done>A single runbook explains how the operator gets from repo checkout to a live proof-of-life deployment on OCI, with the registry-backed deploy path and required OCI inputs stated explicitly rather than left as open choices.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions->OCI API | CI/CD credentials cross from GitHub-hosted runners into OCI control plane operations here |
| GitHub Actions->OCI VM over SSH | Deployment commands and runtime updates cross into the public VM here |
| local env/docs->workflow inputs | Misclassified secrets or variables can leak if the config contract is wrong |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | I | `docs/configuration-contract.md` and workflow env handling | mitigate | Separate Secrets from Variables and examples, and keep sensitive OCI key material only in GitHub Secrets. |
| T-01-08 | E | `.github/workflows/deploy.yml` | mitigate | Scope OCI operations to the project compartment and document the API-key auth seam for later OIDC replacement. |
| T-01-09 | T | `scripts/deploy-vm.sh` | mitigate | Verify the deployed stack with an nginx-fronted `/health` check before marking deploy success. |
</threat_model>

<verification>
Run the shared CI validation script locally, inspect the workflows for the expected triggers and `ghcr.io` publish/pull wiring, and confirm the runbook covers the proof-of-life exit gate through nginx on OCI using repo-level GitHub Secrets/Variables as the baseline contract.
</verification>

<success_criteria>
Phase 1 has an executable delivery spine: explicit configuration contracts, CI checks on pull requests, and a documented merge-to-deploy path that publishes a prebuilt app image to `ghcr.io` and proves the live OCI runtime through nginx.
</success_criteria>

<output>
After completion, create `.planning/phases/01-delivery-spine-and-oci-bootstrap/01-04-SUMMARY.md`
</output>
