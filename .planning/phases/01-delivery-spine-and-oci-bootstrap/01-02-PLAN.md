---
phase: 01-delivery-spine-and-oci-bootstrap
plan: 02
type: execute
wave: 3
depends_on:
  - 01-01
files_modified:
  - app/Dockerfile
  - deploy/compose/compose.prod.yaml
  - deploy/nginx/nginx.conf
  - scripts/validate-runtime.sh
autonomous: true
requirements:
  - PLAT-03
must_haves:
  truths:
    - "Operator can run the proof-of-life Next.js app in the same two-container topology intended for OCI."
    - "Public HTTP traffic terminates at nginx and reaches the Next.js app container through committed runtime assets."
    - "The runtime foundation exposes a health path that later deploy automation can probe without adding catalog features early."
  artifacts:
    - path: "app/Dockerfile"
      provides: "Container build for the Next.js runtime"
    - path: "deploy/compose/compose.prod.yaml"
      provides: "Two-container runtime definition matching D-07 and D-08"
    - path: "deploy/nginx/nginx.conf"
      provides: "Reverse proxy from nginx to the app container"
    - path: "scripts/validate-runtime.sh"
      provides: "Local smoke path for the runtime topology"
  key_links:
    - from: "deploy/nginx/nginx.conf"
      to: "app/app/health/route.ts"
      via: "nginx proxy_pass to the app service"
      pattern: "/health"
    - from: "deploy/compose/compose.prod.yaml"
      to: "app/Dockerfile"
      via: "compose build context or image reference for the app service"
      pattern: "app:"
---

<objective>
Commit the OCI-shaped container runtime assets for Phase 1 so the later deploy plan can target the locked `nginx` -> `Next.js` topology instead of inventing a runtime shape midstream.

Purpose: Satisfy D-07, D-08, and D-09 with committed runtime assets that build on the proof-of-life app from `01-01`.
Output: An app Dockerfile, nginx proxy config, production-oriented compose definition, and a runtime validation script.
</objective>

<execution_context>
@/home/jgreenwa/.codex/get-shit-done/workflows/execute-plan.md
@/home/jgreenwa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-delivery-spine-and-oci-bootstrap/01-CONTEXT.md
@.planning/phases/01-delivery-spine-and-oci-bootstrap/01-RESEARCH.md
@.planning/phases/01-delivery-spine-and-oci-bootstrap/01-01-PLAN.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the OCI-shaped two-container runtime assets</name>
  <read_first>
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-CONTEXT.md
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-RESEARCH.md
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-01-PLAN.md
  </read_first>
  <files>
    app/Dockerfile
    deploy/compose/compose.prod.yaml
    deploy/nginx/nginx.conf
    scripts/validate-runtime.sh
  </files>
  <acceptance_criteria>
    - `test -f app/Dockerfile && test -f deploy/compose/compose.prod.yaml && test -f deploy/nginx/nginx.conf` succeeds.
    - `rg -n "nginx|proxy_pass|health|3000|80" deploy/nginx/nginx.conf deploy/compose/compose.prod.yaml` confirms nginx fronts the app service and exposes the proof path.
    - `rg -n "docker compose|curl|/health" scripts/validate-runtime.sh` confirms the runtime smoke command exists.
  </acceptance_criteria>
  <action>Implement the locked runtime shape from D-07, D-08, and D-09 with committed container assets only: an app Dockerfile, an nginx config that proxies to the app container, and a production-oriented compose file for one VM. Build directly on the proof-of-life app from `01-01` rather than redefining the application scaffold here. Add a small runtime validation script that builds or starts the stack and curls the proof endpoint through nginx. Keep the image references and compose inputs configurable enough for the later deploy workflow to inject registry-hosted images, but do not introduce extra containers, sidecars, or direct Object Storage access.</action>
  <verify>
    <automated>bash scripts/validate-runtime.sh</automated>
  </verify>
  <done>The repository contains the exact two-container runtime topology planned for OCI, and local verification exercises the proof-of-life route through nginx rather than hitting the app container directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser->nginx | Untrusted public HTTP requests enter the deployed runtime here |
| nginx->next-app | Reverse-proxy traffic crosses from the public entrypoint to the private app container |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T | `deploy/nginx/nginx.conf` | mitigate | Route traffic only to the app container and expose only the nginx listener publicly. |
| T-01-05 | D | `deploy/compose/compose.prod.yaml` | mitigate | Keep the app container off the public port map so traffic must traverse nginx. |
| T-01-06 | I | `app/Dockerfile` | accept | The container build should contain only application runtime assets and no secrets. |
</threat_model>

<verification>
Run the runtime smoke script and confirm it reaches `/health` through nginx rather than curling the app service directly.
</verification>

<success_criteria>
The committed runtime assets honor the locked two-container OCI shape and provide a local proof path that matches the later production deployment topology.
</success_criteria>

<output>
After completion, create `.planning/phases/01-delivery-spine-and-oci-bootstrap/01-02-SUMMARY.md`
</output>
