---
phase: 01-delivery-spine-and-oci-bootstrap
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/deploy.yml
  - .github/actions/setup-runtime/action.yml
  - scripts/validate-ci.sh
  - scripts/deploy-vm.sh
  - docs/configuration-contract.md
  - docs/deployment-runbook.md
  - .env.example
  - .github/.env.github.example
autonomous: true
requirements:
  - PLAT-02
  - PLAT-03
user_setup:
  - service: github-actions
    why: "Repository environments, secrets, and variables must be populated before deploy automation can run."
    env_vars:
      - name: OCI_CLI_USER_OCID
        source: "OCI Console -> User details for the deploy identity"
      - name: OCI_TENANCY_OCID
        source: "OCI Console -> Tenancy details"
      - name: OCI_FINGERPRINT
        source: "OCI API signing key metadata"
      - name: OCI_PRIVATE_KEY_PEM
        source: "Generated API signing private key stored as a GitHub Secret"
      - name: OCI_REGION
        source: "OCI region hosting the Phase 1 resources"
      - name: OCI_HOME_REGION
        source: "OCI tenancy home region for region-sensitive resources"
      - name: OCI_COMPARTMENT_OCID
        source: "Terraform-managed or operator-selected project compartment"
      - name: VM_PUBLIC_IP
        source: "Output from Terraform apply for the runtime VM"
must_haves:
  truths:
    - "Operator can configure local development and GitHub delivery using a committed contract that separates secrets, variables, and example files."
    - "Pull requests run automated repository validation before merge."
    - "Merges to `main` have a committed deployment path that can publish the two-container runtime and verify proof-of-life through nginx on OCI."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Pull request validation workflow"
    - path: ".github/workflows/deploy.yml"
      provides: "Main-branch deployment workflow"
    - path: "docs/configuration-contract.md"
      provides: "Source-of-truth config split for local env, GitHub Secrets, and GitHub Variables"
    - path: "scripts/deploy-vm.sh"
      provides: "VM deployment entrypoint used by GitHub Actions"
    - path: "docs/deployment-runbook.md"
      provides: "Operator runbook for end-to-end deployment and smoke verification"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/validate-ci.sh"
      via: "workflow job invoking the shared validation script"
      pattern: "validate-ci"
    - from: ".github/workflows/deploy.yml"
      to: "scripts/deploy-vm.sh"
      via: "main-branch deployment job"
      pattern: "deploy-vm"
    - from: "docs/configuration-contract.md"
      to: ".env.example"
      via: "documented local env contract"
      pattern: "GitHub Secrets"
---

<objective>
Turn the app/runtime and OCI baseline into a real delivery spine by defining the config contract, GitHub Actions workflows, and operator runbook for proof-of-life deployment.

Purpose: Satisfy PLAT-02 and finish the Phase 1 exit gate from D-16 and D-17 with a concrete PR-validation and main-branch deploy path.
Output: Config contract docs, CI workflow, deploy workflow, shared scripts, and the runbook for deploying the nginx -> Next.js stack to OCI.
</objective>

<execution_context>
@/home/jgreenwa/.codex/get-shit-done/workflows/execute-plan.md
@/home/jgreenwa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-delivery-spine-and-oci-bootstrap/01-CONTEXT.md
@.planning/phases/01-delivery-spine-and-oci-bootstrap/01-RESEARCH.md
@.planning/phases/01-delivery-spine-and-oci-bootstrap/01-01-PLAN.md
@.planning/phases/01-delivery-spine-and-oci-bootstrap/01-02-PLAN.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the committed configuration and secret contract</name>
  <read_first>
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-CONTEXT.md
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-RESEARCH.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>
    docs/configuration-contract.md
    .env.example
    .github/.env.github.example
  </files>
  <acceptance_criteria>
    - `test -f docs/configuration-contract.md && test -f .env.example && test -f .github/.env.github.example` succeeds.
    - `rg -n "GitHub Secrets|GitHub Variables|local env|replaceable auth|OCI API signing keys" docs/configuration-contract.md` confirms the split and the future-auth seam from D-10 through D-12.
    - `rg -n "OCI_REGION|OCI_HOME_REGION|OCI_COMPARTMENT_OCID|VM_PUBLIC_IP" .env.example .github/.env.github.example` confirms the explicit variable contract for local and GitHub use.
  </acceptance_criteria>
  <action>Create the source-of-truth configuration contract for Phase 1. Split values across committed examples, GitHub Secrets, and GitHub Variables exactly as required by D-10 through D-12: sensitive OCI signing credentials and SSH material in Secrets, non-sensitive deployment coordinates in Variables/example files, and local development values in a committed `.env.example`. Document the current API-key-based auth path as replaceable so execution can later swap to OIDC or another short-lived auth flow without redesigning the workflow inputs.</action>
  <verify>
    <automated>rg -n "GitHub Secrets|GitHub Variables|replaceable|OCI_API|OCI_REGION|VM_PUBLIC_IP" docs/configuration-contract.md .env.example .github/.env.github.example</automated>
  </verify>
  <done>The repository has one explicit, committed config contract covering local operation and GitHub automation, and it preserves the planned auth replacement seam.</done>
</task>

<task type="auto">
  <name>Task 2: Add PR validation and main-branch deployment workflows</name>
  <read_first>
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-RESEARCH.md
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-01-PLAN.md
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-02-PLAN.md
  </read_first>
  <files>
    .github/workflows/ci.yml
    .github/workflows/deploy.yml
    .github/actions/setup-runtime/action.yml
    scripts/validate-ci.sh
    scripts/deploy-vm.sh
  </files>
  <acceptance_criteria>
    - `test -f .github/workflows/ci.yml && test -f .github/workflows/deploy.yml && test -f scripts/deploy-vm.sh` succeeds.
    - `rg -n "pull_request|main|terraform|docker|ssh|health" .github/workflows/ci.yml .github/workflows/deploy.yml scripts/validate-ci.sh scripts/deploy-vm.sh` confirms validation and deploy behavior.
    - `rg -n "OCI_PRIVATE_KEY_PEM|OCI_FINGERPRINT|OCI_REGION|VM_PUBLIC_IP" .github/workflows/deploy.yml` confirms the workflow consumes the committed contract instead of hard-coded values.
  </acceptance_criteria>
  <action>Implement the GitHub delivery spine. The PR workflow should install runtime dependencies and run the repository validation contract, including app checks and Terraform validation. The `main` deploy workflow should use the signing-key model from D-10, build or publish the two-container runtime assets, apply Terraform as needed, deploy the runtime to the single OCI VM over SSH, and probe the nginx-fronted proof endpoint before success. Keep image and auth inputs parameterized so the workflow can later move from API keys to OIDC and can switch image registry strategy without rewriting the whole pipeline.</action>
  <verify>
    <automated>bash scripts/validate-ci.sh</automated>
  </verify>
  <done>Pull requests and `main` have committed automation paths, and the deploy automation targets the real Phase 1 runtime and infrastructure artifacts rather than placeholders.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the operator deployment runbook and smoke path</name>
  <read_first>
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-CONTEXT.md
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-RESEARCH.md
    - docs/configuration-contract.md
  </read_first>
  <files>
    docs/deployment-runbook.md
  </files>
  <acceptance_criteria>
    - `test -f docs/deployment-runbook.md` succeeds.
    - `rg -n "proof-of-life|nginx|/health|GitHub Actions|terraform apply|docker compose" docs/deployment-runbook.md` confirms the runbook covers the full delivery path and exit gate.
    - `rg -n "home region|registry|fallback|unknown" docs/deployment-runbook.md` confirms the runbook accounts for the known open seams without blocking the phase.
  </acceptance_criteria>
  <action>Write the Phase 1 operator runbook that ties the whole bootstrap sequence together: local validation, OCI bootstrap prerequisites, GitHub secret/variable setup, expected workflow triggers, deploy execution, and proof-of-life verification through nginx on the OCI VM. Explicitly record the handled unknowns called out by research, including home-region-sensitive OCI values and the chosen-but-replaceable image publication strategy, so Phase 1 can execute without hidden decisions surfacing mid-run.</action>
  <verify>
    <automated>rg -n "proof-of-life|GitHub Actions|nginx|/health|home region|registry" docs/deployment-runbook.md</automated>
  </verify>
  <done>A single runbook explains how the operator gets from repo checkout to a live proof-of-life deployment on OCI, including the remaining configurable seams and how they are intentionally isolated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions->OCI API | CI/CD credentials cross from GitHub-hosted runners into OCI control plane operations here |
| GitHub Actions->OCI VM over SSH | Deployment commands and runtime updates cross into the public VM here |
| local env/docs->workflow inputs | Misclassified secrets or variables can leak if the config contract is wrong |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | I | `docs/configuration-contract.md` and workflow env handling | mitigate | Separate Secrets from Variables and examples, and keep sensitive OCI key material only in GitHub Secrets. |
| T-01-08 | E | `.github/workflows/deploy.yml` | mitigate | Scope OCI operations to the project compartment and document the API-key auth seam for later OIDC replacement. |
| T-01-09 | T | `scripts/deploy-vm.sh` | mitigate | Verify the deployed stack with an nginx-fronted `/health` check before marking deploy success. |
</threat_model>

<verification>
Run the shared CI validation script locally, inspect the workflows for the expected triggers and env wiring, and confirm the runbook covers the proof-of-life exit gate through nginx on OCI.
</verification>

<success_criteria>
Phase 1 has an executable delivery spine: explicit configuration contracts, CI checks on pull requests, and a documented merge-to-deploy path that can prove the live OCI runtime through nginx.
</success_criteria>

<output>
After completion, create `.planning/phases/01-delivery-spine-and-oci-bootstrap/01-03-SUMMARY.md`
</output>
