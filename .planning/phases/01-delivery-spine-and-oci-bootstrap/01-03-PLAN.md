---
phase: 01-delivery-spine-and-oci-bootstrap
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/terraform/providers.tf
  - infra/terraform/versions.tf
  - infra/terraform/locals.tf
  - infra/terraform/variables.tf
  - infra/terraform/main.tf
  - infra/terraform/outputs.tf
  - infra/terraform/bootstrap/backend.hcl.example
  - infra/terraform/bootstrap/imports.md
  - infra/terraform/environments/prod/terraform.tfvars.example
  - docs/oci-bootstrap.md
  - docs/terraform-state.md
  - .gitignore
autonomous: false
requirements:
  - PLAT-01
must_haves:
  truths:
    - "Operator can bootstrap the OCI baseline with minimal manual work and hand long-term ownership to Terraform."
    - "Terraform state has a documented path from short-lived local bootstrap state to OCI Object Storage remote state with versioning."
    - "The OCI baseline uses compartment-scoped access instead of root-scoped routine deploy permissions."
  artifacts:
    - path: "infra/terraform/main.tf"
      provides: "The committed OCI baseline for Phase 1"
    - path: "infra/terraform/bootstrap/backend.hcl.example"
      provides: "Remote backend contract for OCI Object Storage state"
    - path: "infra/terraform/bootstrap/imports.md"
      provides: "Import path for any manually created bootstrap resources per D-03"
    - path: "docs/oci-bootstrap.md"
      provides: "Operator runbook for minimal manual OCI bootstrap"
    - path: "docs/terraform-state.md"
      provides: "State migration and versioning guidance"
  key_links:
    - from: "docs/oci-bootstrap.md"
      to: "infra/terraform/bootstrap/imports.md"
      via: "manual bootstrap steps followed by Terraform import"
      pattern: "terraform import"
    - from: "infra/terraform/bootstrap/backend.hcl.example"
      to: "docs/terraform-state.md"
      via: "backend configuration instructions"
      pattern: "bucket"
---

<objective>
Define the OCI/Terraform bootstrap system for Phase 1, including the minimal-manual bootstrap flow, remote state strategy, and compartment-scoped infrastructure baseline.

Purpose: Satisfy D-01 through D-03 and D-13 through D-15 with committed Terraform and operator runbooks rather than loose notes.
Output: Terraform baseline files, backend/bootstrap examples, import guidance, and operator docs for OCI bootstrap and state migration.
</objective>

<execution_context>
@/home/jgreenwa/.codex/get-shit-done/workflows/execute-plan.md
@/home/jgreenwa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-delivery-spine-and-oci-bootstrap/01-CONTEXT.md
@.planning/phases/01-delivery-spine-and-oci-bootstrap/01-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: Confirm Terraform is installed before Terraform-based verification</name>
  <files>None — prerequisite checkpoint only</files>
  <action>Check whether `terraform` is available on the execution machine before starting the infrastructure work in this plan. If `terraform version` fails, install Terraform manually and confirm the binary is on `PATH` before continuing. Do not start Task 1 verification, `fmt`, or `validate` until this prerequisite is satisfied, because Phase 1 treats Terraform as the ownership model per D-02 and there is no practical non-Terraform fallback.</action>
  <verify>
    <automated>terraform version</automated>
  </verify>
  <done>The executor has a working `terraform` CLI available locally, so the plan's Terraform formatting and validation steps can run without improvising a setup path mid-execution.</done>
</task>

<task type="auto">
  <name>Task 1: Create the compartment-scoped Terraform baseline</name>
  <read_first>
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-CONTEXT.md
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-RESEARCH.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>
    infra/terraform/providers.tf
    infra/terraform/versions.tf
    infra/terraform/locals.tf
    infra/terraform/variables.tf
    infra/terraform/main.tf
    infra/terraform/outputs.tf
    infra/terraform/environments/prod/terraform.tfvars.example
    .gitignore
  </files>
  <acceptance_criteria>
    - `test -f infra/terraform/main.tf && test -f infra/terraform/variables.tf && test -f infra/terraform/environments/prod/terraform.tfvars.example` succeeds.
    - `rg -n "compartment|instance|vcn|subnet|network_security_group|bucket|versioning" infra/terraform/*.tf` finds the Phase 1 OCI baseline components.
    - `rg -n "\\.terraform|terraform.tfstate" .gitignore` confirms local Terraform state and working directories are ignored.
  </acceptance_criteria>
  <action>Author the first committed OCI baseline under `infra/terraform/` so Terraform becomes the long-term source of truth per D-02. Model the minimum Phase 1 footprint only: the project compartment or compartment inputs, least-privilege IAM/policy seams, network baseline, single VM runtime target, and the Object Storage pieces needed for remote state. Make home-region and tenancy-specific naming explicit variables instead of hidden assumptions so Always Free and home-region constraints do not block planning. Exclude Oracle schema/app data concerns from this phase, but leave outputs and naming conventions that later phases can consume.</action>
  <verify>
    <automated>terraform -chdir=infra/terraform fmt -check && terraform -chdir=infra/terraform validate</automated>
  </verify>
  <done>The repository contains a coherent OCI Terraform baseline for the bootstrap phase, with explicit variable contracts and least-privilege scope boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Document bootstrap, import, and remote state migration</name>
  <read_first>
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-CONTEXT.md
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-RESEARCH.md
    - .planning/codebase/INTEGRATIONS.md
  </read_first>
  <files>
    infra/terraform/bootstrap/backend.hcl.example
    infra/terraform/bootstrap/imports.md
    docs/oci-bootstrap.md
    docs/terraform-state.md
  </files>
  <acceptance_criteria>
    - `test -f docs/oci-bootstrap.md && test -f docs/terraform-state.md && test -f infra/terraform/bootstrap/imports.md` succeeds.
    - `rg -n "manual|import|terraform init -migrate-state|versioning|home region" docs/oci-bootstrap.md docs/terraform-state.md infra/terraform/bootstrap/imports.md` finds the bootstrap flow, import guidance, state migration, and region caveats.
    - `rg -n "backend.*oci|bucket|namespace|key" infra/terraform/bootstrap/backend.hcl.example` confirms the remote backend contract is explicit.
  </acceptance_criteria>
  <action>Write the operator-facing bootstrap docs that keep manual OCI work to the smallest possible surface per D-01 and D-03. Spell out the exact local-state-first bootstrap, the creation or import of any bootstrap-only bucket resources, the switch to the OCI backend with bucket versioning, and the post-bootstrap expectation that Terraform owns the footprint. Include prerequisites such as Terraform installation, the optional-but-helpful OCI CLI, and how to handle OCI home-region naming without guessing. Keep the import guidance concrete enough that the executor can immediately apply it when a manual resource is unavoidable.</action>
  <verify>
    <automated>rg -n "terraform import|terraform init -migrate-state|versioning|home region" docs/oci-bootstrap.md docs/terraform-state.md infra/terraform/bootstrap/imports.md infra/terraform/bootstrap/backend.hcl.example</automated>
  </verify>
  <done>The bootstrap path is documented end to end: minimal manual OCI setup, Terraform import for any manually created resources, and a clear migration into OCI Object Storage remote state with versioning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub/Operator->OCI IAM | Deploy credentials and bootstrap credentials cross into OCI control plane here |
| Terraform local->OCI Object Storage backend | Sensitive infrastructure state moves from local bootstrap to remote managed storage here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | E | `infra/terraform/main.tf` | mitigate | Model compartment-scoped IAM and policies instead of root-scoped routine deploy privileges. |
| T-01-05 | T | `docs/terraform-state.md` | mitigate | Require OCI backend migration with bucket versioning and document `terraform init -migrate-state` as the steady-state path. |
| T-01-06 | I | `backend.hcl.example` | mitigate | Commit only example backend config and keep real sensitive values out of version control. |
</threat_model>

<verification>
First satisfy the Terraform prerequisite checkpoint, then run Terraform formatting and validation against the committed baseline. After that, grep the docs to confirm they explicitly cover manual bootstrap boundaries, import, remote backend migration, and home-region handling.
</verification>

<success_criteria>
Phase 1 has a real OCI/Terraform bootstrap blueprint that honors the locked minimal-manual ownership model and leaves no ambiguity about how the project moves into Terraform-managed remote state.
</success_criteria>

<output>
After completion, create `.planning/phases/01-delivery-spine-and-oci-bootstrap/01-03-SUMMARY.md`
</output>
