---
phase: 01-delivery-spine-and-oci-bootstrap
plan: 01
type: execute
wave: 2
depends_on:
  - 01-03
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - app/package.json
  - app/tsconfig.json
  - app/next.config.ts
  - app/next-env.d.ts
  - app/app/layout.tsx
  - app/app/page.tsx
  - app/app/health/route.ts
  - app/public/.gitkeep
autonomous: true
requirements:
  - PLAT-03
must_haves:
  truths:
    - "Operator has a minimal Next.js proof-of-life app committed as the foundation for the OCI runtime."
    - "The repository exposes both a human-facing proof page and a machine-checkable health route before infrastructure wiring begins."
    - "Later runtime and deploy plans can target stable app entrypoints without pulling catalog scope into Phase 1."
  artifacts:
    - path: "app/app/page.tsx"
      provides: "Proof-of-life page for the OCI bootstrap exit gate"
    - path: "app/app/health/route.ts"
      provides: "Machine-checkable health endpoint for local and remote smoke checks"
    - path: "app/package.json"
      provides: "App-local dependency and script contract for the Next.js workspace"
    - path: "package.json"
      provides: "Root workspace and toolchain entrypoint"
  key_links:
    - from: "package.json"
      to: "app/package.json"
      via: "workspace package management for the single-repo app layout"
      pattern: "workspace"
    - from: "app/app/page.tsx"
      to: "app/app/layout.tsx"
      via: "App Router layout wrapping the proof-of-life page"
      pattern: "layout"
---

<objective>
Create the proof-of-life Next.js scaffold for Phase 1 so the later runtime, OCI, and CI/CD plans can target a real application instead of placeholders.

Purpose: Establish the first real application artifact inside the single-repo structure required by D-04 and D-05.
Output: A minimal Next.js app workspace with a proof-of-life page and a health endpoint.
</objective>

<execution_context>
@/home/jgreenwa/.codex/get-shit-done/workflows/execute-plan.md
@/home/jgreenwa/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-delivery-spine-and-oci-bootstrap/01-CONTEXT.md
@.planning/phases/01-delivery-spine-and-oci-bootstrap/01-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the proof-of-life Next.js application</name>
  <read_first>
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-CONTEXT.md
    - .planning/phases/01-delivery-spine-and-oci-bootstrap/01-RESEARCH.md
    - .planning/ROADMAP.md
  </read_first>
  <files>
    package.json
    pnpm-workspace.yaml
    app/package.json
    app/tsconfig.json
    app/next.config.ts
    app/next-env.d.ts
    app/app/layout.tsx
    app/app/page.tsx
    app/app/health/route.ts
    app/public/.gitkeep
  </files>
  <acceptance_criteria>
    - `rg -n "next|react|typescript" package.json app/package.json` shows the committed app/tooling manifest.
    - `test -f app/app/page.tsx && test -f app/app/health/route.ts` succeeds.
    - `rg -n "Autographs|proof-of-life|ok" app/app/page.tsx app/app/health/route.ts` finds the human page copy and machine health response.
  </acceptance_criteria>
  <action>Create a single-repo workspace foundation per D-04 and D-05 with the first real `Next.js` app under `app/`. The app must stay intentionally narrow for Phase 1: one layout, one proof-of-life landing page, and one `/health` route that returns a stable success response for later deploy smoke tests. Do not pull Oracle, Object Storage, admin auth, or gallery behavior into this scaffold. Keep the health contract simple and machine-readable so nginx and GitHub deploy verification can target it directly.</action>
  <verify>
    <automated>corepack pnpm install && corepack pnpm --filter app lint && corepack pnpm --filter app typecheck</automated>
  </verify>
  <done>The repository contains a runnable Next.js app with a proof-of-life page and a health endpoint, and the root workspace definition is ready for later CI and deploy tasks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor->workspace manifests | Dependency and script definitions enter the future build and runtime path here |
| browser->next app | Untrusted HTTP requests reach the proof-of-life page and health route here once deployed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `package.json` and `app/package.json` | mitigate | Keep the scaffold dependency set minimal and explicit so later workflow validation can install a predictable toolchain. |
| T-01-02 | D | `app/app/health/route.ts` | mitigate | Return a lightweight health response with no external dependencies so probes remain reliable during bootstrap. |
| T-01-03 | I | `app/app/page.tsx` | accept | The proof page is intentionally non-sensitive and should not include secrets or OCI metadata. |
</threat_model>

<verification>
Run the workspace install plus app lint/typecheck. Confirm the scaffold exposes both the human proof page and the machine-readable `/health` route without introducing infrastructure or feature scope early.
</verification>

<success_criteria>
The first executable application assets exist in the repo, and they provide the stable proof-of-life surface that later runtime, infrastructure, and deployment plans will target.
</success_criteria>

<output>
After completion, create `.planning/phases/01-delivery-spine-and-oci-bootstrap/01-01-SUMMARY.md`
</output>
