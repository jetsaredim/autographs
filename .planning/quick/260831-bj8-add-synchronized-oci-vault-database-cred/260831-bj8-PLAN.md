---
quick_id: 260831-bj8
status: complete
mode: validate
description: Add synchronized OCI Vault database credential refresh and one retry on ORA-01017 without enabling automatic rotation
must_haves:
  truths:
    - Long-running Oracle catalog and heartbeat connections refresh the database password from the configured OCI Vault secret only after an exact ORA-01017 database error.
    - Concurrent failures from the same credential generation cause at most one Vault read and all waiters reuse the refreshed snapshot.
    - Each failed operation retries connection at most once, and unrelated Oracle, Vault, session-initialization, query, and application errors are never retried as credential failures.
    - Direct-password deployments and startup schema bootstrap preserve their current behavior.
    - Automatic OCI secret rotation and its schedule remain disabled until the recovery path is proven live.
    - Each OCI Vault-backed runtime secret load logs only its logical kind, numeric Vault bundle version, and requested CURRENT stage; database refresh logs also correlate the controller generation with the new Vault version.
  artifacts:
    - controller/src/oracle_credentials.rs
    - controller/src/oracle_connection.rs
    - controller/src/oracle_catalog.rs
    - controller/src/oracle_heartbeat.rs
    - controller/src/runtime_secrets.rs
    - controller/src/routes.rs
    - docs/configuration-contract.md
    - docs/deployment-runbook.md
    - .planning/spikes/004-configuration-secret-boundary/CONFIGURATION-BOUNDARY.md
    - controller/Cargo.toml
    - controller/Cargo.lock
    - .planning/spikes/001-oracledb-container-smoke/Cargo.toml
    - .planning/spikes/001-oracledb-container-smoke/Cargo.lock
  key_links:
    - The startup Vault resolution retains the Oracle password secret OCID as a refresh coordinate without retaining another password copy in ControllerConfig.
    - Oracle connection attempts return the credential generation and typed failure classification needed by the shared async recovery helper.
    - The catalog repository and heartbeat both use that recovery helper before running their normal blocking Oracle work.
---

# Quick Task 260831-bj8 Plan

## Task 1: Make the database credential provider refreshable and single-flight

**Files:** `controller/src/oracle_credentials.rs`, `controller/src/oci_secrets.rs`, `controller/src/runtime_secrets.rs`, `controller/src/config.rs`, `controller/src/routes.rs`

**Action:** Extend credential snapshots with a monotonically increasing generation and add an optional asynchronous refresh source backed by the already configured OCI Vault secret OCID. Serialize refreshes and re-check the generation after acquiring the refresh lock so concurrent ORA-01017 failures trigger one Vault read. Reject blank refreshed values, replace the shared snapshot atomically under a short lock, and log only generation/outcome metadata. Preserve an immutable provider when no Vault secret reference is configured.

**Verify:** Focused unit tests prove stable snapshots, successful replacement, blank/failing refresh preservation, direct-password no-refresh behavior, and one source read across concurrent stale-generation requests.

**Done:** A shared provider can safely replace its current password exactly once per failed generation without exposing or duplicating credential values across callers.

## Task 2: Add exact ORA-01017 recovery and one bounded retry

**Files:** `controller/src/oracle_connection.rs`, `controller/src/oracle_catalog.rs`, `controller/src/oracle_heartbeat.rs`

**Action:** Preserve the Oracle driver's typed `ErrorKind` long enough to classify only a `DbError` beginning with `ORA-01017:`. Add one shared asynchronous connection helper that attempts with a credential snapshot, refreshes only for that exact failure, then retries once with the current snapshot. Return all other failures immediately and never retry session initialization or catalog/query operations. Route long-running repository and heartbeat connections through the helper while leaving synchronous schema bootstrap unchanged.

**Verify:** Unit tests with injected connect behavior prove exact classification, a single retry after refresh, no retry for unrelated/malformed errors, refresh failure propagation, and no second refresh/retry after the replacement credential fails.

**Done:** Existing catalog and heartbeat traffic can recover from a coordinated ADB/Vault password rotation without a controller restart, while all retry behavior remains narrow and bounded.

## Task 3: Incorporate the Rust dependency updates from PR #228

**Files:** `controller/Cargo.toml`, `controller/Cargo.lock`, `.planning/spikes/001-oracledb-container-smoke/Cargo.toml`, `.planning/spikes/001-oracledb-container-smoke/Cargo.lock`

**Action:** Apply only PR #228's Rust crate updates: Argon2 0.5.3 to 0.6.0 and UUID 1.24.1 to 1.26.0 in every active or retained Rust manifest that declares them, then regenerate the corresponding lockfiles. Keep the Hadolint action, Terraform CLI/provider, and Rust container-image changes in PR #228; they are outside the requested Rust dependency scope.

**Verify:** Run the controller's complete default and production-feature test/check/Clippy suite plus `cargo check --manifest-path .planning/spikes/001-oracledb-container-smoke/Cargo.toml`. Confirm lockfiles resolve Argon2 0.6.0 and UUID 1.26.0 and no unrelated direct dependency changed.

**Done:** The current PR subsumes PR #228's Rust crate updates without taking ownership of its unrelated action, Terraform, or container-image updates.

## Task 4: Validate the production-feature runtime contract and open the audit-trail PR

**Files:** `docs/configuration-contract.md`, `docs/deployment-runbook.md`, `.planning/spikes/004-configuration-secret-boundary/CONFIGURATION-BOUNDARY.md`, current task files, and GitHub PR evidence

**Action:** Document the exact failure-driven refresh/retry boundary and the restart-only behavior that remains for other secrets. Run formatting, focused tests, all controller tests, production-persistence check/test/Clippy, runtime validation, and diff hygiene. Commit and push the implementation and open a normal ready-for-review PR. Do not enable an OCI rotation schedule or claim live rotation proof in this task.

**Verify:** `cargo fmt --manifest-path controller/Cargo.toml --check`; focused and full `cargo test`; `cargo check --features production-persistence`; `cargo clippy --features production-persistence --all-targets -- -D warnings`; `bash scripts/validate-runtime.sh`; and `git diff --check` all pass, followed by green PR checks.

**Done:** The bounded recovery implementation is reviewable, fully validated locally and in CI, and ready for a controlled manual rotation test after deployment.

## Task 5: Expose safe OCI Vault bundle version observability

**Files:** `controller/src/oci_secrets.rs`, `controller/src/runtime_secrets.rs`, `controller/src/config.rs`, `controller/src/routes.rs`, `controller/src/oracle_credentials.rs`, `controller/src/oracle_connection.rs`, `docs/configuration-contract.md`, `docs/deployment-runbook.md`, `.planning/spikes/004-configuration-secret-boundary/CONFIGURATION-BOUNDARY.md`

**Action:** Decode the required numeric OCI secret-bundle `versionNumber` with each `CURRENT`-stage value. Log the logical secret kind, bundle version, and constant requested stage after each successful startup resolution. Retain only the database password's numeric Vault version in its shared credential snapshot, propagate it through single-flight refresh outcomes, and add it to successful refresh/reuse logs beside the controller-local generation. Never log secret values, secret OCIDs, user-defined version names, or customer metadata.

**Verify:** Unit tests cover bundle metadata decoding, version propagation through startup overrides and credential snapshots, successful and concurrent refresh outcomes, and direct-password credentials without Vault metadata. Run the complete production-feature test/Clippy/runtime validation suite and a GitHub reviewer/fixer cycle until the PR has no actionable findings.

**Done:** Operators can correlate controller credential refreshes with durable OCI Vault bundle versions without increasing secret exposure or per-connection logging volume.
