---
quick_id: 260827-vd8
status: complete
mode: validate
description: "C4 slice 1: disable controller and host core-dump persistence and Kdump, add executable contracts, and document the later encrypted-swap and OLED boundaries"
must_haves:
  truths:
    - The generated controller Quadlet sets both the soft and hard core-file limits to zero.
    - systemd-coredump neither stores nor processes userspace core bodies.
    - Kdump is stopped and masked, and crashkernel is removed from installed boot entries without an automatic reboot.
    - CI proves the runtime wiring and fails if persistence-control tasks are skipped or errors are ignored.
    - Encrypted swap conversion, production reboot proof, wallet tmpfs, and OLED reclamation remain explicit later work and are not claimed complete by this slice.
  artifacts:
    - deploy/ansible/roles/autographs_deploy/tasks/kernel_persistence.yml
    - deploy/ansible/roles/autographs_deploy/templates/autographs-coredump.conf.j2
    - deploy/ansible/roles/autographs_deploy/templates/autographs-controller.container.j2
    - controller/tests/runtime_kernel_persistence.rs
    - docs/deployment-runbook.md
    - docs/configuration-contract.md
  key_links:
    - The deploy role includes the kernel-persistence task file, which renders the coredump policy and controls Kdump.
    - The executable contract test binds the Ansible task/template wiring to the generated controller Quadlet.
    - Operator documentation requires an ordinary deploy plus an approved reboot before the live C4 core/Kdump gate is considered proven.
---

# Quick Task 260827-vd8 Plan

## Task 1: Manage controller core limits, systemd-coredump, and Kdump

**Files:** `deploy/ansible/roles/autographs_deploy/defaults/main.yml`, `deploy/ansible/roles/autographs_deploy/tasks/main.yml`, `deploy/ansible/roles/autographs_deploy/tasks/kernel_persistence.yml`, `deploy/ansible/roles/autographs_deploy/templates/autographs-coredump.conf.j2`, `deploy/ansible/roles/autographs_deploy/templates/autographs-controller.container.j2`

**Action:** Add `LimitCORE=0` to the generated controller Quadlet so both soft and hard limits are zero. Render a systemd-coredump drop-in with `Storage=none` and `ProcessSizeMax=0`, reload systemd when it changes, and include the dedicated kernel-persistence task file from the normal deploy role. Stop and mask Kdump without ignoring failures. Remove `crashkernel` arguments from installed boot entries with `grubby` only when they are present, record that a reboot is required, and never reboot automatically. Preserve historical crash/core files. Do not change swap, PCP, `/var/oled`, LVM, mounts, or filesystems in this slice.

**Verify:** Run deploy playbook syntax-check and Ansible lint, inspect the generated configuration contracts, and confirm no reboot or destructive storage operation is present.

**Done:** A normal deploy establishes the non-persistent userspace-core policy and disables Kdump in current service state while safely staging the boot-argument change for an operator-approved reboot.

## Task 2: Add executable contracts and CI coverage

**Files:** `controller/tests/runtime_kernel_persistence.rs`, `.github/workflows/ci.yml`, `scripts/validate-runtime.sh`

**Action:** Add fast source-contract tests proving the controller core limit, paired coredump settings, task inclusion, non-ignored Kdump disablement, conditional `crashkernel` removal, and absence of automatic reboot. Extend CI/runtime validation to execute the new test and verify every referenced template/task path. Add negative assertions that this C4 slice contains no swap conversion or OLED/LVM/filesystem mutation.

**Verify:** Run `cargo test --manifest-path controller/Cargo.toml --test runtime_kernel_persistence`; `bash scripts/validate-runtime.sh`; the deploy playbook Ansible syntax check; `ansible-lint deploy/ansible/`; and `git diff --check`.

**Done:** CI rejects regressions that permit controller/user core persistence, leave Kdump enabled, ignore persistence-control failures, or smuggle storage reclamation into this slice.

## Task 3: Document deployment, reboot proof, and later C4/C5 boundaries

**Files:** `docs/deployment-runbook.md`, `docs/configuration-contract.md`, `.planning/spikes/004-configuration-secret-boundary/README.md`, `.planning/spikes/004-configuration-secret-boundary/CONFIGURATION-BOUNDARY.md`, `.planning/spikes/007-enforceable-cleanup-plan/CLEANUP-PLAN.md`, `.planning/quick/260827-vd8-c4-kernel-persistence-gates-disable-cont/260827-vd8-RESEARCH.md`

**Action:** Document exact post-deploy checks for controller core limits, systemd-coredump policy, Kdump service state, `kexec_crash_loaded`, installed `crashkernel` arguments, and the operator-approved reboot checkpoint. Record the completed C3 proof, but do not mark all of Spike 004 validated. State that the next C4 slice implements encrypted boot-only-key swap and must repeat the memory-safety preflight after stopping the Autographs services, immediately before `swapoff`; production cutover and reboot evidence follow that implementation. Record PCP/`/var/oled` analysis as a separate C5 decision and opt-in maintenance operation.

**Verify:** Run `rg -n 'LimitCORE|Storage=none|ProcessSizeMax=0|kexec_crash_loaded|crashkernel|PENDING|encrypted swap|/var/oled|PCP' docs .planning/spikes/004-configuration-secret-boundary .planning/spikes/007-enforceable-cleanup-plan`; `! rg -n 'lvremove|lvreduce|xfs_growfs|umount /var/oled' deploy/ansible/roles/autographs_deploy deploy/ansible/playbooks`; and `git diff --check`.

**Done:** Operators have exact live verification commands and clear boundaries: this slice does not claim encrypted swap, reboot proof, wallet tmpfs, or OLED reclamation.
