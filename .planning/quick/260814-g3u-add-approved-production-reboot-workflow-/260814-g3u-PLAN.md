# Quick Task 260814-g3u: Add Approved Production Reboot Workflow

## Goal

Add a separately approved production reboot path for security findings that need a reboot and old installonly kernel cleanup before OpenSCAP can report clean results.

## Tasks

1. Add the reboot approval workflow and Ansible playbook.
   - Files: `.github/workflows/reboot-security-runtime.yml`, `deploy/ansible/playbooks/security-reboot.yml`
   - Verify: workflow syntax and Ansible syntax checks include the new playbook.

2. Add security patching role tasks/templates for reboot validation, reboot/health/installonly cleanup, and issue refresh/closure.
   - Files: `deploy/ansible/roles/security_patching/tasks/*.yml`, `deploy/ansible/roles/security_patching/templates/*.j2`, defaults.
   - Verify: fixture playbook proves remaining findings refresh and clean scan closure branches.

3. Update docs/tests/ownership.
   - Files: `docs/security-patching.md`, `.github/CODEOWNERS`, `.github/workflows/ci.yml`, `scripts/test_security_patching_create_issue_tasks.py`
   - Verify: Python tests, Ansible fixture tests, syntax, lint.
