---
quick_id: 260920-gz7
description: Enforce a UEK-only production kernel posture across deployment and security patching
status: planned
must_haves:
  truths:
    - Production deployment refuses to mutate kernel packages unless both the running and default kernels are UEK.
    - Deployment removes the explicitly managed RHCK boot/development package family and prevents its reinstallation without excluding shared kernel tools or UEK packages.
    - Security scanning reports installed RHCK packages as UEK-only configuration drift and never offers update or reboot approval for that state.
    - Stale non-rescue boot entries whose kernel image no longer exists are removed, while rescue entries are preserved.
  artifacts:
    - deploy/ansible/roles/autographs_deploy/tasks/kernel_persistence.yml
    - deploy/ansible/roles/security_patching/tasks/scan.yml
    - deploy/ansible/roles/security_patching/tasks/classify_findings.yml
    - deploy/ansible/roles/security_patching/templates/security-report.md.j2
    - deploy/ansible/playbooks/runtime-kernel-persistence-validate-test.yml
    - deploy/ansible/playbooks/security-finding-classification-validate-test.yml
  key_links:
    - Deployment applies the kernel posture before ordinary package installation.
    - OpenSCAP classification combines vulnerability findings with the independently observed installed-kernel posture.
    - Refreshed update and reboot issue state preserves kernel-posture facts after rescans.
---

# Quick Task 260920-gz7 Plan

## Task 1: Converge deployment on UEK-only kernel state

Add fail-closed running/default UEK checks, remove only the managed RHCK package set, persist exact DNF exclusions, and remove stale missing non-rescue boot entries. Extend the structural Ansible validation playbook and operator runbook.

## Task 2: Make security patching classify RHCK as configuration drift

Inventory installed RHCK packages during every scan, prioritize UEK-only drift over DNF update/reboot classification, restrict reboot eligibility to UEK kernel packages, and render actionable drift guidance in the scanner issue and result refresh paths.

## Task 3: Verify the operational contract

Run Ansible syntax checks, focused validation playbooks, and ansible-lint. Confirm no rescue-image deletion is introduced and shared `kernel-headers`, `kernel-tools`, and `kernel-tools-libs` remain outside the removal/exclusion list.
