---
quick_id: 260903-on3
status: in_progress
---

# Grant the Rotation Resource Principal Read Access to the ADB Password Secret

## Task 1: Add the least-privilege tenancy policy

- Add a dedicated tenancy-root IAM policy granting `use secret-family` to the Vault secret resource principal used by coordinated rotation so it can read and update the secret.
- Restrict the caller to Vault secret resource principals in the project compartment and the target to the generated ADB password secret's stable name.
- Grant the same resource principal `use autonomous-databases` only for the `adminPassword` action needed to apply the pending secret version to ADB.
- Avoid coupling the tenancy stack to the deployment-managed secret OCID.
- Add a Terraform contract test that prevents either grant from reverting to the ineffective Database service principal or becoming unscoped.

## Verification

- Run `terraform fmt -check` for both Terraform roots.
- Run `terraform validate` for the tenancy root.
- Run the focused Rust Terraform contract test.
