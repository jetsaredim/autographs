---
quick_id: 260903-on3
status: in_progress
---

# Grant OCI Database Service Read Access to the ADB Password Secret

## Task 1: Add the least-privilege tenancy policy

- Add a dedicated tenancy-root IAM policy for the OCI Database service.
- Limit `read secret-family` to the generated ADB password secret's stable name.
- Add a Terraform contract test that prevents the grant from becoming unscoped or covering the other runtime secrets.

## Verification

- Run `terraform fmt -check` for both Terraform roots.
- Run `terraform validate` for the tenancy root.
- Run the focused Rust Terraform contract test.

