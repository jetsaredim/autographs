---
status: complete
quick_id: 260802-jm4
created: 2026-08-02
---

# Quick Task 260802-jm4: Harden apply security update issue metadata validation

Fix the apply-side security patch approval validation failure where Ansible could see the metadata marker on issue #198 but extract zero scanner metadata blocks before any package updates were attempted.

## Tasks

1. Replace the brittle folded `set_fact` metadata extraction with a quoted extraction task that preserves the regex result under Ansible 2.21.
2. Add apply-side metadata extraction diagnostics and a local fixture playbook that exercises the same role task without posting to GitHub.
3. Wire the metadata validation fixture into CI and run focused Python/Ansible validation, including a read-only Ansible 2.21 live issue parse check.
