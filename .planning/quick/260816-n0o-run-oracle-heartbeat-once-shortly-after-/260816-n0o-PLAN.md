---
quick_id: 260816-n0o
status: planned
created: 2026-08-16
---

# Quick Task 260816-n0o: Run Oracle Heartbeat After Startup

Change the Oracle catalog heartbeat so a deployed or restarted controller proves the keepalive path immediately instead of waiting one full interval before the first SQL command.

## Tasks

1. Update the heartbeat scheduler to run the first heartbeat immediately after startup, then continue on `AUTOGRAPHS_ORACLE_HEARTBEAT_INTERVAL_SECONDS`.
2. Add focused tests for the initial tick behavior while keeping the existing interval parsing contract intact.
3. Verify the production-persistence build and heartbeat tests.

## Verification

- `cargo fmt --check --manifest-path controller/Cargo.toml`
- `cargo test --manifest-path controller/Cargo.toml oracle_heartbeat --features production-persistence`
- `cargo check --manifest-path controller/Cargo.toml --features production-persistence`
