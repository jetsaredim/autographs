---
quick_id: 260830-s49
status: complete
implementation_commits:
  - 43bab87
completed: 2026-08-31
---

# Quick Task 260830-s49 Summary: Share Complete Oracle Connection Settings

Centralized the controller's complete Oracle connection configuration in one immutable shared allocation without adding credential refresh, retry, or rotation behavior.

## Changes

- Stored `OracleConnectionSettings` behind one `Arc` shared by schema bootstrap, the catalog repository, and the heartbeat.
- Removed `Clone` from both the secret-bearing settings and Oracle repository types.
- Changed catalog operations and heartbeat ticks to clone only the shared pointer instead of copying usernames, passwords, connect strings, wallet paths, wallet passwords, and unrelated repository fields.
- Moved all five Oracle values out of `ControllerConfig` during production repository construction so Axum state does not retain and clone a second credential-bearing configuration; retained the existing `oracle_configured` health result.
- Added focused tests proving repository allocation identity and complete removal of Oracle values from long-lived router configuration.
- Deliberately left Vault refresh, `ORA-01017` recovery, and rotation behavior unchanged for a later isolated change.

## Validation

- `cargo fmt --all -- --check` — passed.
- `cargo test` — passed.
- `cargo test --features production-persistence` — passed, including both new ownership tests.
- `cargo check --features production-persistence` — passed.
- `cargo clippy --all-targets --features production-persistence -- -D warnings` — passed.
- `bash scripts/validate-runtime.sh` — passed.
- `git diff --check` — passed.
