---
status: complete
quick_id: 260830-vp0
created: 2026-08-31
---

# Quick Task 260830-vp0: Introduce a static database credential provider

Move the Oracle database password behind a shared snapshot provider while preserving startup-only credential loading and all existing connection behavior.

## Tasks

1. Add a concrete immutable database credential provider whose snapshots share one credential allocation and expose no refresh or mutation behavior.
2. Route `OracleConnectionSettings` and every Oracle connection attempt through the provider while keeping the complete settings allocation shared across schema bootstrap, repository operations, and heartbeat.
3. Add focused ownership and snapshot tests, then run formatting, default and production-feature tests, runtime validation, production checks, and Clippy.
