---
quick_id: 260820-un9
status: complete
mode: quick
description: Remove the redundant host cargo build from PR CI while retaining compiler, test, Clippy, and Docker release-image validation
must_haves:
  truths:
    - PR CI does not link the production controller twice on separate runners.
    - Formatting, tests, production-feature compilation, Clippy, and the real release image build remain required.
  artifacts:
    - .github/workflows/ci.yml
  key_links:
    - The production-feature cargo check validates adapter compilation and the Docker build validates release linking and packaging.
---

# Quick Task 260820-un9 Plan

## Task 1: Remove the redundant host build

**Files:** `.github/workflows/ci.yml`

**Action:** Delete the standalone debug `cargo build --features production-persistence` step from `controller-checks`. Keep formatting, ordinary tests, production-feature `cargo check`, Clippy, and the separate BuildKit release-image build unchanged and parallel.

**Verify:** Run actionlint or the repository workflow check, inspect the workflow diff, and confirm the Docker bake target still builds `controller/Dockerfile` with `production-persistence` in release mode.

**Done:** CI performs no redundant host link while preserving every distinct validation gate.
